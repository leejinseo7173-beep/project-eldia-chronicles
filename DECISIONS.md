# DECISIONS.md — 미정 사항 결정 기록

> 기획서에 없는 세부 사항을 스스로 결정한 근거 로그 (개발 프롬프트 "미정 사항 처리 규칙").

## M1 코어

- **RNG 알고리즘 = xmur3(문자열 해시) + mulberry32**: 의존성 없이 수십 줄로 구현 가능하고 통계 품질이 게임용으로 충분하며, 32bit 정수 상태 하나만 저장하면 세이브에서 완전 재현 가능해서 선택.
- **RNG 상태 저장 API(getState/setState) 선제 포함**: M2 이후 전투 리플레이·시뮬 재현에 필수라 코어 단계에서 계약을 고정.
- **세이브 키 구조 = `eldia:save:{슬롯}` / `eldia:save:{슬롯}:auto` / `eldia:settings`**: 슬롯 3 + 슬롯별 자동저장(기획서 §15)을 localStorage 키 네임스페이스로 직역. 포맷에 `version` 필드를 두고 마이그레이션 지점을 미리 마련.
- **이어하기 = 수동/자동 중 더 최근 저장본 자동 선택**: 선택 UI를 하나 줄이고 "가장 최근 진행"이라는 직관과 일치. 크래시 복구도 자동 커버.
- **내보내기 포맷 = 사람이 읽을 수 있는 JSON**(수동+자동 동봉): 싱글 게임이라 난독화 이득이 없고, 버그 리포트·백업에 유리.
- **새 게임 시드 = `eldia-{슬롯}-{Date.now()}`**: 플레이어마다 다른 여정을 보장하면서 세이브에 시드가 남아 전체 재현 가능. (Math.random 미사용 규칙 준수)
- **정수배 스케일링 예외**: 창이 960×540보다 작을 때만 비율 유지 축소 허용. 정수배만 고집하면 작은 창에서 화면이 잘리기 때문. 창이 충분히 크면 항상 정수배 — 수동 배율(2배/3배)이 창을 초과하면 창에 들어가는 최대 정수배로 클램프한다 (비정수 축소 금지).
- **Galmuri 폰트는 아직 미동봉**: 파일 다운로드는 사용자 승인 필요 사항이라 M1은 `Galmuri9 → Malgun Gothic → monospace` 폰트 스택 폴백으로 처리. 폰트 파일을 `assets/fonts/`에 넣으면 @font-face 연결 예정 (승인 대기).
- **타이틀 화면 픽셀아트 = 240×135 저해상도로 그린 뒤 4배 정수 확대**: 도트가 굵은 픽셀아트 룩을 내면서 프리렌더 비용 최소화. 시드 RNG(`title-bg`)로 그려 항상 같은 그림.
- **덮어쓰기/삭제 확인 모달의 기본 포커스 = "아니요"**: 파괴적 동작 실수 방지.
- **개발 콘솔 토글 = `(백쿼트)/F1**: 한국어 키보드에서도 안정적인 물리 키 코드(e.code) 기준. F5는 브라우저 새로고침과 충돌하므로 수동 저장은 S 키로.
- **효과음 5종(이동/결정/취소/불가/저장)만 M1에서 구현**: AudioContext는 첫 사용자 입력 때 생성(브라우저 자동재생 정책으로 인한 콘솔 경고 방지). BGM·전투음은 M8.
- **UI 공용 문자열(챕터·직업 라벨 등)은 `src/data/strings.js`**: 데이터 주도 원칙을 UI 표기에도 적용, 화면 간 표기 불일치 방지.
- **balance.js에 기획서 확정 수치(상성·방향·치명·스탯표·상태이상·뽑기 등)를 선입력**: M2 이후 매직 넘버가 코드에 스며드는 것을 원천 차단하기 위해 코어 단계에서 단일 지점을 먼저 확립.

## M1 리뷰 반영 (다각도 코드 리뷰 확정 9건 중 8건 수정, 1건 안건화)

- **새 게임 시작 시 슬롯 완전 초기화**: `startNewGame`이 `deleteSlot` 후 저장 — 이전 여정의 자동저장이 새 여정에 섞여 남는 문제 제거.
- **키 입력 필터**: 수정키(Ctrl/Meta/Alt) 조합은 무시(브라우저 단축키 충돌 방지), OS 키 반복은 방향키만 허용(Enter 홀드로 빈 슬롯에 새 게임이 생성되는 사고 방지).
- **세이브 스키마 검증**: `migrate`가 version 정수 검사 + 필수 필드(heroName/seed/chapter/playTimeSec) 타입 검증. 손상 파일 가져오기·손상 localStorage 로드를 차단.
- **타이틀 배경 팔레트 준수**: 배경 픽셀아트를 공용 팔레트 32색 내부 색으로 재작업 (기획서 §16 "팔레트 = 아트 통일성의 핵심" 준수, 팔레트 이탈 선례 차단).
- **changeScene 큐잉**: 페이드 중 전환 요청은 드랍하지 않고 목적지 교체 — M3 오토/소탕의 프로그램적 씬 체인에서 무음 소실로 인한 소프트락 예방.
- **플레이 시간 일괄 누적**: 씬의 `isGameplay` 플래그를 보고 Game.update가 한 곳에서 누적 — 이후 게임플레이 씬마다 누적 코드를 복제하다 누락하는 사고 방지.
- **개발 콘솔 슬롯 삭제 시 타이틀 캐시 동기화** + 이어하기 진입 시에도 항상 슬롯 목록 갱신.
- **Galmuri 폰트 동봉(해결)**: 사용자 승인 후 jsDelivr 공식 CDN(galmuri@2.40.3)에서 Galmuri11/Galmuri11-Bold/Galmuri9 woff2를 `assets/fonts/`에 동봉하고 @font-face 연결. 본문 = Galmuri11, 중간 크기 = Galmuri9 2배.
- **폰트 크기 스냅 티어**: 픽셀 폰트는 원본(9/11px)의 정수배에서만 선명하므로 `font()`가 요청 크기를 11/18/22/33/44/55 티어로 스냅. 호출부는 의도 크기를 쓰고 실제 렌더 크기는 한 곳에서 통제.
- **file:// 직접 실행 가드**: index.html을 더블클릭하면 ES 모듈이 CORS로 차단되어 검은 화면이 되므로, file:// 감지 시 실행 방법 안내를 표시. 더블클릭 실행용 `게임실행.bat`(서버 기동+브라우저 열기) 동봉.

## M2 전투 프로토타입

- **협공 속도 보정 = 속도 1당 +0.5%p** (속도 10 = 위력 55%): 기획서의 "50% + 속도 비례 보정"을 구체화. 도적·궁수의 협공 가치를 살리되 과하지 않게.
- **협공은 방향 배율 미적용(정면 고정)·치명 없음·반격 없음**: 기획서 명시(치명·반격 없음)에 방향 미적용을 추가 — 본 공격의 배후 보너스와 이중 보상 방지. 협공 위력 강화는 M3의 표식(2배)이 담당.
- **반격은 항상 정면 판정**: 공격자는 공격 시 대상을 자동으로 바라보므로 반격이 배후·측면일 수 없음. 반격 치명은 허용(아군 반격만 — 적→아군 치명 없음 규칙이 우선).
- **대각 동률(|dx|=|dy|) 방향 판정 = 측면**: 4방향 배율 체계에서 대각은 정면도 배후도 아니라는 직관.
- **최종 데미지 최소 1 보장**: 최소 보장(공격력 10%)이 반올림으로 0이 되는 저레벨 구간 방지.
- **배치 단계는 M3로 연기**: CLAUDE.md의 M2 정의(이동·공격·방향·협공·반격·2vs2)에 배치가 없어 고정 시작 위치 사용. M3 별점·승리조건 변형과 함께 구현.
- **M2 적 AI = 돌격형만**: 5성향은 M3. 협공·반격 로직은 완전 대칭(적도 협공·반격 가능 — 기획서 "적도 협공 가능").
- **전투 중 저장 미지원**: 저장은 거점·타이틀 시점만(기획서 §15 자동저장 시점과 일치). 전투 상태는 RNG 인스턴스를 포함하므로 직렬화는 필요해지는 시점(M9 파일 저장)에 추가.
- **전투 셀 64px** (= 32×32 스프라이트의 2배 정수 스케일): 8×8=512px 격자가 960×540에서 좌우 정보 패널 공간을 남기는 최대 정수배.
- **해골 병사 스탯 역산**: 정면 일반공격 2회로는 못 잡고(9×2=18 < HP20) 측면·배후·협공을 쓰면 2행동 처치가 되도록 — M2의 학습 목표(방향·협공이 이득임을 몸으로 느끼기)를 스탯으로 강제. 아군 생존은 주인공 기준 5~6대.

## M2 리뷰 반영 (다각도 리뷰 확정 10건 전부 수정)

- **협공자·반격자도 방향 전환을 로직에 영구 반영**: 연출로만 돌리고 로직은 안 돌리면 재생 종료 시 방향이 스냅되는 잔상이 남고, 화면과 실제 판정이 어긋난다. 협공/반격하러 돌아선 유닛은 그 방향으로 남는 것이 전술적으로도 자연스러움(배후 노출 리스크 = 협공 배치의 대가).
- **enemyPhase의 유닛 스킵은 continue**: 승패 확정만 팀 턴 중단 사유. 죽었거나 계획 불가한 적 1기가 나머지 적 턴을 삼키던 잠복 결함 수정 (M3 다수 적·원거리에서 표면화 예정이던 것).
- **적 AI 공격 위치 = 맨해튼 링 전체 열거**: 십자 4방향만 보던 것을 |dx|+|dy| ∈ [min,max] 전체로 — M3 원거리 적(사거리 2~3)이 데이터 추가만으로 올바르게 동작해야 하는 계약(데이터 주도) 이행. 공격 판정도 이동 후 실거리 기준으로 단순화.
- **최종 데미지 하한을 BALANCE.damage.floor로 이동**: 절대 규칙 2(모든 밸런스 수치는 balance.js) 준수.
- **BattleScene.enter()에서 pendingEnemy 등 잔존 상태 전부 리셋**: 씬 싱글턴 재사용 구조에서 적 턴 대기 타이머가 새 전투로 새어 들어가 적이 선제 행동하던 버그 수정.
- **'대기' 경유 방향 선택은 ESC/우클릭으로 액션 메뉴 복귀 허용**: 아무 것도 실행되지 않았으므로 취소 가능해야 함(기획서 §6 단계별 취소). 공격 경유는 이미 실행됐으므로 취소 불가 유지.
- **방향 선택 진입 직후 0.2초 화살표 입력 유예**: 이동/타겟 단계에서 누르고 있던 방향키의 OS 반복이 방향을 즉시 확정해버리는 오입력 방지.
- **방향 선택 버튼 화면 내 클램프 + 패널 위에 렌더**: 가장자리 행에서 버튼이 화면 밖으로 잘리거나 정보 패널에 가려지던 문제 — 마우스 완결 조작(절대 규칙 8) 복원.
- **턴 표시는 재생 완료 기준(statusLine)**: 로직이 선반영되어 적 턴 연출 중에 "다음 턴"으로 미리 넘어가 보이던 표시 어긋남 수정.
- **협공 전용 효과음 배선**: 만들어 두고 안 쓰던 'assist' 차임을 협공 임팩트에 연결 — 본 공격과 청각적으로 구분.

## M2 전투 구조 전환 — JRPG 사이드뷰 (사용자 지시)

- **8×8 격자 전투를 사이드뷰 고정 진형으로 전면 교체**: 사용자가 "좌우에 있는 JRPG 형태"를 명시적으로 요청. 기획서·CLAUDE.md의 전투 관련 조항을 전부 개정했고, 별도 사본(-2 폴더)은 사용자 지시로 무시·미참조. 격자 버전 전체는 커밋 b455367에 보존.
- **연계 협공으로 협공 재설계**: 위치 기반 협공(반대편·측면)의 대체 — 같은 팀 턴에 이미 공격받은 대상을 다른 아군이 다시 치면 직전 공격자가 자동 추가타(50%+속도 보정, 치명·반격 없음, 공격 1회당 1회). "누가 먼저 치고 누가 잇는가"의 순서 퍼즐로 협공의 재미를 계승. 표식(2배)·궁수 협공 패시브와의 시너지 유지. 연계 추적은 팀 턴 단위로 리셋.
- **반격 = 일반공격 피격 후 생존 시 무조건** (사거리·배후 조건 소멸): "일반공격은 공짜지만 반격 위험" 선택 구조 유지. 스킬·협공 피격은 반격 불가.
- **스킬 대상 규격 6종으로 개정**: 적 단일/적 2체(대상+HP 최저 자동)/적 전체/아군 단일/아군 전체/자신. "적 2체"의 추가 대상을 HP 최저로 고정해 무작위 대상 없이 저RNG 원칙(계획 100% 실행) 유지. 격자 범위(십자·관통·원형)를 쓰던 스킬 45종 전부 재배정.
- **이동력·사거리 스탯 삭제 (6종→5종)**: 도적·궁수의 기동 정체성은 속도로 계승(치명·협공 위력·상태이상 관여). 실피 패시브는 "자신보다 느린 적에게 +20%"로, 리안 바람걸음은 속도 버프로 재해석.
- **상태이상 재정의**: 둔화 = 속도 −30% / 속박 = 일반공격 외 행동 불가 / 신속 = 속도 +30%.
- **보스 기믹 격자 의존 2종 교체**: 1장 선봉장 "정면 반감" → "매 턴 첫 피격 반감"(집중 공격·연계 튜토리얼), 4장 수호자 "방향 회전" → "약점 속성 순환". 종장 카르노스 2P도 약점 순환으로.
- **해골 병사 사이드뷰 재역산**: HP [12,+2] — 동레벨 주인공 일반공격 정확히 2회. 혼자 잡으면 반격을 더 맞고, 나눠 치면 연계가 마무리 — 집중 공격 학습 유도. 생존 앵커(주인공 5~6대)는 유지.
- **스테이지 배치는 src/data/stages.js**: 전투 씬은 스테이지 ID만 받아 조립 — 콘텐츠 추가가 데이터 추가로 끝나는 계약을 M2부터 고정.
- **일반공격 유효성은 canAttack 단일 관문**: 불법 호출은 RangeError — UI·오토·시뮬 어느 경로도 규칙 우회 불가.
- **전투 스프라이트 3배 정수 확대(96px)**: 사이드뷰 4vs4가 960×540에서 겹치지 않는 최대 정수배.
- **★ 에셋 방침 개정 (사용자 확정, 2026-07-31)**: 절대 규칙 1의 "외부 에셋 금지"를 3분할로 대체.
  - **코드 생성**: 스프라이트·타일·이펙트·UI·효과음 — 포즈 추가 시 15명 자동 적용, 팔레트 통일이 강점. 외부 팩으로는 직업·무기 구성이 맞는 것을 찾기 어려움
  - **자체 제작(작화 담당자)**: VN 초상·컷신 일러스트·타이틀 아트. 96×96 얼굴이 코드 생성 한계가 가장 큰 지점인데, 자체 작화면 **라이선스 자유 + 한 사람이 그리니 그림체 통일 보장 + 스팀 AI 신고 불필요**
  - **외부 에셋 = BGM만**: 칩튠으로 감동적 멜로디가 불가한 유일한 영역. CC0 우선, CC-BY는 `CREDITS.md` 기록, 상업 이용 가능한 것만
  - 그 외 외부 팩(스프라이트·UI 등)은 **여전히 금지** — 그림체 불일치가 균일한 소박함보다 나쁨
  - **작화 규격**: 96×96 픽셀아트 버스트업, 표정 3종(기본/웃음/진지), **공용 팔레트 32색+속성색 6 준수**, 투명 PNG. 대상 = 동료 15 + 주인공 3직업×남녀 + 세레스 + 주요 NPC. M6 착수 전 규격서 전달
  - **적용 시점**: M6 착수 전 규격 전달, 그때까지 스프라이트 확대본을 임시 사용. 연출 코드는 이미지 소스만 교체 가능한 구조로
- ~~에셋 방향 = 코드 생성 유지 (2026-07-30)~~ → 위 항목으로 대체됨: 캐릭터·배경·사운드 전부 기획서의 '외부 에셋 제로' 원칙대로 코드 생성으로 진행. 단 **M6 수직 슬라이스에서 VN 초상(96×96×15명×표정3) 실물을 보고 재평가** — 품질이 부족하면 초상·타이틀 일러스트·BGM만 외부/AI 에셋으로 보강하는 혼합안을 그때 검토. 생성 지점이 일원화되어 있어 교체 비용은 낮다.
## M3 전투 완성

- **광역 스킬 계수 원칙 = 전체기 60~65%·2체기 80~85% (단일기 대비)**: 시뮬에서 성물 해방(전체 150%)+버프 스택이 적 전원을 한 방에 쓸어버리는 붕괴 확인 → 일괄 하향. 기획서 §3·§4의 명시 계수보다 낮지만, 기획서 헤더의 "모든 수치는 초기값이며 balance.js에서 일괄 조정" 원칙을 따름.
- **기절·속박은 적용 시 +1턴 내부 보정**: 상태 지속은 피해자 팀 턴 시작 틱에서 감소하므로, 보정 없이는 상대가 건 기절이 발동 전에 만료됨. 도트(화상·재생)는 틱 시 발동 후 감소라 보정 불필요 — 정확히 2회 발동.
- **도발 = 구현상 버프 분류**: 시전자 자신에게 걸어 적의 단일 공격을 강제하는 상태이므로 정화(디버프 해제) 대상이 아님. 보스 면역은 "보스가 도발 강제를 무시"로 처리.
- **화상 도트 = 시전자 공·마 중 높은 쪽 30%**: 기획서 "시전자 마·공 30%"의 구체화 — 물리·마법 시전자 모두 유효하게.
- **협공 트리거 = 적 단일 대상 데미지 스킬(일반+액티브)**: 광역기는 연계를 트리거·갱신하지 않음 — 광역+연계 이중 이득 방지. 협공 추가타 속성은 협공자 일반공격(물리/마법)을 따름. 반격도 반격자 일반공격의 물리/마법을 따름.
- **은신 전원 상태 등 극단 상황에서는 단일 지정 허용**: 대상이 아예 없어 행동 불가가 되는 소프트락 방지.
- **스테이지 적 레벨 오버라이드**: 정예·보스 역할과 버티기 위협은 적 개별 level 필드로 TTK 역산 (데이터 주도 — 코드 수정 없음). 버티기 스테이지는 5턴 내 전멸 불가 레벨로 설정해 생존 경로가 실제 승리 루트가 되게 함 (시뮬 승률 88%·평균 5.7턴 확인).
- **오토 우선순위 = 치유(60% 이하)→지원(보호막·버프·디버프)→공격 스킬(광역은 적 2체+)→일반공격(처치 가능>연계>점수식)**: 기획서 "오토는 수동보다 약간 비효율" 허용 범위. 세라핀 HP 30% 이하 소모 스킬 금지 반영.
- **전투 아이템 액션은 M5로**: 소모품·인벤토리가 M5 범위라 액션 메뉴에서 제외. 기획서 M3 정의에도 아이템 없음.
- **성물 스킬은 M3에서 무속성**: 속성 모드는 M4 정령 시련 해금 후. 데이터 훅(모드별 부가효과)은 유지.
- **시뮬 결과 해석 기준**: 4인 파티 vs 일반몹 5기가 평균 2턴 종료는 "딜러 2행동" 앵커의 산술적 귀결로 정상. 전투 길이 3~5분은 연출 시간+적 웨이브·구성(M4+ 콘텐츠)으로 달성.

## 전투 수치 표시 원칙 추가 (사용자 지시, 기획서 §6에 명문화)

- **모든 판단 근거를 숫자로 노출한다**: 저RNG(회피·미스 없음) 설계라 결과가 전부 계산 가능하므로, 플레이어가 추측하게 두지 않는다. 적 스탯도 아군과 동일하게 공개 — 정보 은폐로 난이도를 만들지 않는다.
- **미구현 항목 (다음 작업 시 반영)**:
  1. **스킬 카드에 실제 예상 수치** — 현재 "150%×2" 같은 계수만 표시. 실제 피해·치유·보호막 수치로 교체(계수는 보조)
  2. **유닛 정보의 실효 스탯 표시** — 현재 기본 스탯만 노출. 버프·디버프 반영값과 증감 표시(`공격 12 → 15 ▲`) 필요
  3. **도트 턴당 예상 피해량** — 화상 등의 실제 피해량이 정보 패널에 안 보임
  4. **보호막 잔량 수치** — 현재 바 형태만, 숫자 없음
  5. **★ 용어·효과 설명 툴팁** — 철벽·도발·은신 등 상태이상 14종, 스킬, 패시브, 스탯의 효과를 이름만으로는 알 수 없다. 마우스 오버 + 키보드 접근 경로 둘 다 필요
     - 툴팁은 **효과 설명 + 그 유닛 기준 실제 수치**(예: "공격·마력 +30% → 12 → 15, 남은 2턴")
     - **한계도 명시**: 도발이 광역기를 못 막음 / 은신이 광역기엔 맞음 — 모르면 속았다고 느끼는 지점
     - 설명 문구는 `data/statuses.js`·`skills.js`에 데이터로, 수치는 `balance.js`에서 채워 넣어 **수치 조정 시 설명이 자동 동기화**되게 (설명·실제 불일치 원천 차단)
     - `STATUSES`에 `desc` 필드 신설 필요 (`PASSIVES`에는 이미 있음)
- 이미 충족된 항목: 대상 선택 예상 결과 패널(피해·치명·협공·반격·처치·상성), 데미지 팝업, 상태 아이콘 남은 턴, 별점 조건 표시.

## M3 리뷰 반영 (확정 5건 + 자체 확인 3건 수정)

- **조건 치명 확정(급소 찌르기)을 예상치·오토 처치 판정에 반영**: 결정론적 확정은 예상 데미지에 정확히 표시(치명 100%) — 저RNG 원칙.
- **skillSelect 단계의 투명 토글 히트박스 제거**: 오토/배속 버튼이 안 그려지는 단계에서 주인공 성물 카드 클릭을 가로채던 문제(High) — 히트 테스트를 렌더 조건과 일치.
- **오토 ON 시 진행 중 수동 선택 정리**: action/skillSelect/target 단계에서 오토를 켜면 select로 정규화해 즉시 구동 — "오토 ON인데 멈춘 것처럼 보이는" 상태 머신 공백 해소.
- **상태 아이콘 칩에 남은 턴 표시** (기획서 §6). 보스 면역의 정적 아이콘 표시는 보스 UI가 생기는 M6에서.
- **하드 집중 가중치·패시브 HP 임계값을 balance.js로 이동** (절대 규칙 2): ai.hardFocusBonus, passives.c11_p/c08_p.hpThreshold.
- **보스 도발 면역은 "강제 무시"로만**: 자기 자신에게 거는 도발까지 applyStatus에서 막혀 수호형 보스가 자기 스킬을 못 쓰던 문제 수정 — 면역 처리는 validTargets(공격자가 보스면 도발 강제 무시)에서만.
- **도르간 방패 밀치기 공격 약화 1턴**: 스킬별 debuffTurns 오버라이드 추가 (기본 2턴과 다른 지속).
- **미완 리뷰 차원**: logic-bugs·ai-bugs 리뷰어가 세션 한도로 미실행 — 두 파일은 자동 테스트 36건+시뮬 320회로 커버됨. 필요 시 한도 리셋 후 추가 감사 가능.

- **기본 모션 세트는 M2 범위** (사용자 피드백 반영): "손맛이 M2의 성패" 기준상 캐릭터 기본 모션은 지금 — 호흡(2프레임, 유닛별 위상차)·돌진 중 달리기 프레임·타격 순간 공격 자세(무기 내지르기)·피격 넉백·전투불능 쓰러짐(회전+페이드). 스킬 파티클·협공 초상 컷인·보스 연출은 계획대로 M3~M8. 스프라이트 규격은 방향별 2프레임 + 공격 자세로 확장(필드 걷기 2프레임 스펙과 공용).

## 전투 스프라이트 파이프라인 (BETA — 원화 반영)

- **포즈 매핑의 기준은 `tools/build_hero_sword.ps1` 한 파일**: 어느 원화가 어느 포즈가 되는지, 방향·정렬·색 보정을 전부 여기 적는다. 원화를 다시 그려도 이 스크립트만 돌리면 세트 전체가 재생성된다.
- **한 캐릭터의 전 프레임은 같은 배율(-Scale)로 변환**: 포즈별로 높이를 58px에 맞추면(-CharHeight) 돌진(bbox 높이 257)·쓰러짐(141)에서 몸이 커졌다 작아진다. 옆모습 선 자세를 58px로 두는 배율을 전 프레임에 고정한다.
- **발끝 y=60 고정**: 전투 앵커가 발끝 기준(ALLY_ANCHORS y=300)이고 렌더가 `py - drawH + 8`이므로, 60이어야 화면에서 정확히 앵커 위에 선다.
- **바운딩 박스는 밀도 임계(1.2%)로 판정**: 원화 배경의 옅은 얼룩 한두 픽셀 때문에 박스가 캔버스 전체로 커져 캐릭터가 깨알같이 작아지던 문제.
- **가로 정렬은 발 무게중심**: 전체 박스 중앙으로 맞추면 검을 뻗은 프레임에서 몸이 옆으로 밀려 애니메이션이 좌우로 떨린다. 단 쓰러짐은 발이 기준이 안 되므로 `-Align bbox`.
- **자동 방향 감지 실패 프레임은 `-Facing flip` 수동 지정**: 망토가 뒤로 길게 뻗은 돌진 포즈는 좌우 폭이 비슷해 자동 판정이 'unknown'이 된다(attack1·attack2·down).
- **`-Tint`로 원화별 조명색 중화**: attack1 원화만 주황 역광이라 금발이 붉게 양자화됐다. 채널 배율로 잡는다.
- **캔버스 96×64** (세로보다 가로가 넓다): 검을 뻗은 프레임이 64폭에서 잘린다.
- **원화는 `assets/sprites/_source/`에 512×768로 보관**: 원본 1024×1536 전량은 36MB라 저장소에 넣기 부담. 최종이 96px이라 512에서 재변환해도 결과가 같고, 12장 1.8MB면 재현성이 확보된다.
- **일러스트(01:47 배치 3장)는 전투 스프라이트로 쓰지 않는다**: 픽셀아트가 아니라 채색 일러스트다. VN 초상·스킬 컷인용으로 남긴다.
- **PowerShell 스크립트는 UTF-8 BOM으로 저장**: BOM이 없으면 Windows PowerShell 5.1이 ANSI로 읽어 한글 주석이 따옴표를 깨뜨린다.
## 원거리 투사체 (BETA — M3 전투 연출)

- **투사체는 스킬 데이터가 소유한다**: `skills.js`에 `shot: { kind, color }`. 종류는 arrow / bolt / orb / volley / wave 5종. 새 캐릭터를 넣을 때 코드가 아니라 데이터 한 줄로 연출이 붙는다 (절대 규칙 3).
- **`melee`와 `shot`은 배타적**: 붙어서 때리거나(가로 돌진) 날려 보내거나 둘 중 하나. 렌더에서도 shot이 있으면 돌진을 강제로 끈다.
- **"도착 = 피해"를 규칙으로 삼고 재생 길이를 역산한다**: 이벤트 길이 = 예비(0.22) + 비행(거리÷속도) + 잔심(0.16). 그래서 뒷열의 먼 적을 쏘면 연출이 실제로 조금 더 길다. 고정 길이로 두면 거리가 달라질 때 화살이 도착하기 전에 피해가 들어가거나 이미 지나간 뒤에 들어간다.
- **비행 시간은 규격값이 아니라 "이 프레임에서 착탄까지 남은 실제 시간"을 쓴다**: 발사·착탄 판정이 각각 프레임 경계로 밀리기 때문에, 규격값을 그대로 쓰면 짧은 사격에서 최대 20px 어긋났다. 실측 잔여시간으로 바꿔 오차 0px.
- **투사체는 속도가 아니라 경로 보간으로 움직인다**: `vx/vy`로 밀면 위 문제가 재발한다. 진행률에서 위치를 직접 구한다.
- **비행 시간 상한 0.42초**: 뒷열↔뒷열(≈755px)은 상한에 걸린다. 더 길게 두면 4인 파티 한 턴이 늘어져 손맛이 죽는다.
- **캔버스 회전(rotate) 대신 픽셀 블록을 늘어놓아 그린다**: 회전 사각형은 안티에일리어싱이 생겨 픽셀아트와 어긋난다 (절대 규칙 7). 진행 방향은 포물선 경로의 접선을 수치 미분해서 구한다.
- **연사(volley)는 마지막 한 발이 정확히 피해 시점에 꽂힌다**: 나머지는 `stagger`만큼 먼저 도착해 "드르륵" 박히는 잔상을 만든다.
- **협공·반격은 그 유닛의 일반공격 규격을 따른다**: 시전 스킬이 따로 없으므로 `skills.basic.shot`을 본다. 궁수가 협공하면 화살이 날아간다.
- **hit/heal은 시전자가 일치할 때만 투사체를 인정한다**: 그러지 않으면 턴 종료 도트·재생 이벤트에 직전 스킬의 투사체가 새어 나온다.
- **착탄 전용 이펙트 `impact` 추가**: 화살처럼 fx가 얌전한 스킬도 꽂히는 순간이 보이게 짧고 날카로운 불꽃 + 작은 링.
- **효과음 `shot` 추가** (audio.js): 시위를 튕기듯 880→1480Hz로 짧게 훑는다.
- 검증: 72전투(1배속·2배속) 588발, 착탄 오차 0px, 진행률 1.0 이상(절대 미달 없음), 잔류 파티클 0, 콘솔 에러 0. 시뮬레이터 4스테이지 × 300회 = 1200전투 무오류 (로직 무영향 확인).
## 치유 연출 — 성스러운 가호 (BETA)

- **치유는 던지는 게 아니라 내려앉는 것**: 처음엔 치유도 투사체(구슬)로 만들었으나 사용자 지적으로 교체. 대상 **발밑**에 마법진이 퍼지고 그 안에서 빛기둥이 솟는 `blessing` 연출로 바꿨다.
- **성직자의 '공격'은 구슬 투척 유지**: 성광·축복탄은 적에게 쏘는 것이므로 orb 그대로. 힐만 가호로 바꾼다.
- **마법진은 유닛보다 먼저 그린다**: 나중에 그리면 링이 정강이를 가로질러 공중에 뜬 것처럼 보인다. 먼저 그리면 캐릭터가 원 안에 서 있는 것으로 읽힌다.
- **원근을 위해 세로 0.34배로 눌린 타원**: 바닥에 누운 원으로 보이게.
- **heal 이벤트에 fx를 실어 보낸다**: 기존 applyImpact가 sparkle/green을 하드코딩해 `skill.fx`가 사장돼 있었다. hit 이벤트와 같은 방식으로 통일.
- **지속 회복(regen) 틱은 작은 반짝임 유지**: 매 턴 터지므로 마법진을 깔면 화면이 시끄럽다.
- **개발 중 주의 — ES 모듈 캐시**: python http.server + 브라우저 모듈 캐시 때문에 코드를 고쳐도 옛 모듈이 그대로 돈다. 검증 중 "고쳤는데 반영이 안 되는" 현상의 원인이었다. 확인할 때는 강한 새로고침(Ctrl+Shift+R)이 필요하다.
## 광역 스킬 동시 발동 · 가호 강화 (BETA)

- **재생 단위를 이벤트 하나에서 "묶음"으로 바꿨다**: 광역 스킬은 대상마다 이벤트가 따로 나오는데 하나씩 재생하면 성역(아군 4인 치유) 한 방이 4.25초를 넘겼다. 같은 시전자의 광역 파동은 한 묶음으로 동시에 재생한다. → 1.55초.
- **대상마다 0.06초 시차**: 완전히 동시에 터지면 밋밋하다. 파도처럼 훑고 지나가게.
- **근접 광역은 묶지 않는다**: 한 몸이 세 방향으로 동시에 돌진할 수 없다. 회오리 베기 같은 근접 전체기는 순차 돌진을 유지한다(3.1초).
- **대상이 하나뿐인 묶음은 쪼갠다**: 단일 대상 연출은 기존 감각을 그대로 둔다. 전투불능·기적 같은 부수 이벤트도 단일 대상에서는 예전처럼 따로 재생된다.
- **연출 검증은 반드시 게임 화면으로 한다**: 이펙트만 따로 확대해 그린 렌더는 실제보다 훨씬 화려해 보인다. 그것만 보고 "됐다"고 판단해 사용자에게 잘못 보고한 적이 있다. 1배 크기·캐릭터에 가려지는 정도·지속 시간까지 실제 화면에서 확인할 것.
- **가호 수치**: 마법진 반경 70 / 지속 1.1초 / 3중 링(후광+몸통+흰 심) + 바닥 빛무리 / 빛기둥 15개(중앙 굵은 축 1 포함) 지속 0.9초.
## 참격 궤적 (BETA)

- **발광은 합성 모드 `lighter`로 낸다**: 후광(넓고 옅게) → 몸통 → 흰 심(좁고 밝게) 세 겹을 겹쳐 그리면 겹치는 만큼 밝아져 **가운데가 저절로 하얗게 타오르고 가장자리로 색이 번진다**. 흰색을 직접 칠하는 것보다 레퍼런스에 가깝고 색만 바꾸면 속성별로 그대로 쓸 수 있다.
- **초승달은 sin 곡선으로 taper**: `sin(πs)^0.65`로 굵기를 주면 가운데가 두껍고 양끝이 뾰족해진다.
- **호의 중심을 벤 방향 반대쪽에 둔다**: 볼록한 면이 진행 방향을 향해야 그쪽으로 휘두른 것처럼 보인다.
- **회전(rotate) 금지**: 투사체와 같은 이유. 픽셀 블록만 늘어놓는다 (절대 규칙 7).
- **연출 수치는 `src/core/fx.js`**: 작업계획.md 1부 §7 기술 메모대로. balance.js는 게임 수치 전용(절대 규칙 2)이고, 연출 타이밍은 강약에 영향을 주지 않으므로 분리한다. 투사체 규격도 여기로 옮겼다. (기술 메모의 "파티클·투사체 오브젝트 풀 재사용"은 아직 미이행)
- **검증 캡처는 8KB 이하로**: 게임 화면을 base64로 옮길 때 크면 꼬리가 잘려 이미지 아래쪽이 깨진다. 깨진 것을 렌더링 버그로 오인한 적이 있다. 의심되면 캔버스 픽셀을 직접 조회해 확인할 것.
## 검기(wave) — 날아가는 초승달 (BETA)

- **참격 궤적의 3중 발광을 `arcGlow()`로 공유한다**: 검기는 "날아가는 참격"이므로 같은 그리기를 쓴다. base 각도를 받게 일반화해서, 참격은 벤 방향(±1), 검기는 경로 접선 방향으로 볼록하게 만든다.
- **호의 중심은 진행 방향 뒤쪽 0.9r**: 볼록한 면이 앞을 향해야 날아가는 것으로 읽힌다.
- 적용: 성검 해방(금색), 절대영도(청록). 나머지 투사체(arrow/bolt/orb/volley)는 블록 꼬리 유지.
- **교훈 — 이름만 붙여놓고 렌더러를 안 만든 것**: `wave`라는 종류를 만들어놓고 실제로는 공용 블록 렌더러를 그대로 썼다. 규격표에 항목이 있다고 구현된 것이 아니다. 종류를 추가할 때는 전용 렌더 분기가 실제로 있는지 확인할 것.
## 성물 등급 연출 (BETA)

- **등급 차이는 이펙트 크기가 아니라 "화면을 장악하느냐"로 낸다**: 성물을 그냥 크게만 하면 여전히 밋밋하다. 배경을 죽이고 시전자만 남기는 순간 무게가 생긴다.
- **수렴 입자는 거리·시차를 흩뜨려야 한다**: 같은 반지름에서 동시에 출발시켰더니 정적인 금색 고리로 보였다. 황금각(2.39996 rad)으로 각도를 흩고, 거리와 출발 시차를 각각 흩어서 빨려드는 것으로 읽히게 했다.
- **히트스톱은 이벤트 시계·파티클만 멎힌다**: 시네마틱 시계(암전·섬광)까지 멎으면 화면이 완전히 굳어 버려 오히려 버벅이는 것처럼 보인다. 둘을 분리한다.
- **히트스톱은 배속 영향을 60%만 받는다** (계획서 1부 §5): 2배속에서 그대로 절반이 되면 손맛이 죽는다.
- **성물 시전 이벤트 길이 1.05초** (일반 스킬 0.4): 연출이 들어갈 자리를 이벤트 길이로 확보한다. 성물 한 방 전체는 약 2.4초.
- 전 등급 히트스톱(일반 4프레임 / 액티브 6 / 성물·치명타 10 / 처치 14)은 계획서 1부 §6에서. 지금은 성물 터지는 순간만.
## 마법 이펙트 고퀄화는 원화 대기 (2026-08-02, 사용자 결정)

- **`burst` / `ring` / `rain`을 3중 발광으로 올리는 작업을 원화 묶음으로 미룬다**: 지금 하면 주인공만 원화 모션이고 나머지 14명은 코드 생성이라, 이펙트만 화려해지면 오히려 화풍 차이가 더 도드라진다. 동료 원화가 모이면 모션(공격 스윙·달리기·피격)과 함께 한 번에 처리한다.
- 대기 묶음: 작업계획.md 1부 §6의 1·4·5번 + 마법 이펙트 고퀄화.
- 지금 진행 가능한 것: 6번(히트스톱 전 등급 확대), 7번(피격 찌그러짐), 8번(스킬 컷인 1단계), 9번(액티브 등급 차등), 10번(소리 3층).
## 히트스톱 · 돌진 모션 곡선 (BETA)

- **광역은 파동 전체에 한 번만 멎는다**: 대상마다 멎으면 4인 광역에서 정지가 4번 겹쳐 턴이 늘어진다. 재생 묶음 단위로 한 번(`pb.stopped`). 다단히트는 대상이 하나라 묶음이 쪼개지므로 매번 멎는다 — 의도한 동작이다.
- **등급 판정은 재생 중인 스킬의 `kind`**: 협공·반격은 시전 스킬이 따로 없으므로 일반공격 취급.
- **돌진에 예비 동작을 넣는다**: 선형 전진→정지→선형 복귀는 미끄러지듯 보인다. 돌진 거리의 12%만큼 뒤로 뺐다가 제곱 가속으로 꽂고, 감속하며 돌아온다. 도달 시점(0.42)을 근접 타격 시점과 같은 상수로 묶었다.
- 계획서 1부 §6의 6번 완료. 남은 것: 7번 피격 찌그러짐, 8번 스킬 컷인, 9번 액티브 등급 차등, 10번 소리 3층.
## 묶음 재생의 인과 순서 (BETA — 버그 수정)

- **묶어서 동시에 재생하면 인과가 깨진다**: 광역 동시 발동을 넣으면서 구성원이 각자 자기 길이대로 돌게 했더니, 짧은 이벤트(전투불능 0.12초)가 긴 이벤트(검기 명중 0.45초)보다 먼저 터졌다. **적이 죽은 뒤에 데미지가 들어갔다.**
- **해결: 발동 시각을 이벤트 순서대로 단조 증가하게 강제한다** (`stepSchedule`). logic.js가 hit → miracle → death 순으로 내보내므로 그 순서가 곧 인과다. 렌더는 그 순서를 절대 뒤집으면 안 된다. 각 구성원의 시작 시각을 역산해 자기 명중 시점이 정해진 시각에 오게 맞춘다.
- **히트스톱도 묶음당 한 번**: 5기 전멸에서 처치 정지(0.233초)가 5번 겹쳐 화면이 1초 넘게 굳었다. 단 더 긴 등급이 나오면 그쪽으로 올린다.
- **교훈**: 연출 최적화(동시 발동)가 게임 로직의 순서 보장을 침범할 수 있다. 이벤트 스트림을 재배열하는 변경은 인과 검사를 함께 넣어야 한다 — 지금은 회귀 테스트에 "death가 그 대상의 hit보다 먼저 적용되는가" 검사를 포함시켰다.
## 히트스톱과 비행 중 투사체 (BETA — 버그 수정)

- **히트스톱은 묶음의 마지막 타격에서만 건다**: 히트스톱은 이벤트 시계와 파티클을 모두 멎게 하므로, 광역에서 첫 명중에 걸면 **아직 공중에 있는 나머지 투사체까지 얼어붙는다.** 검기 5개가 날아가다 멈췄다 다시 가는 것으로 보였다. 마지막 명중 = 모든 투사체가 착탄한 시점이라 얼릴 것이 없다.
- 등급은 묶음 안에서 가장 긴 것을 쓴다(처치 > 치명 > 성물 > 액티브 > 일반). 단일 대상은 구성원이 하나뿐이라 기존 동작과 같다.
- **"렉 같다"는 신고를 렌더 비용으로 넘겨짚지 말 것**: 실측하니 렌더 중앙값 0.1ms, 최대 fillRect 413개 프레임도 2.6ms로 60fps 한계(16.7ms)에 한참 못 미쳤다. 원인은 전혀 다른 곳(히트스톱)이었다.
- 회귀 검사에 상시 포함: **"정지 중인데 공중에 투사체가 있는 프레임"** 과 **"투사체 위치가 도착 전에 멈춘 프레임"** 을 센다.
## 파티 편성 · 전투 일시정지 메뉴 · 개발 서버 (BETA)

- **편성 화면은 밸런스 튜닝의 선행 작업**: 임의 파티로 전투를 못 돌리면 협공 공식도 적 스탯도 검증할 수 없다. 작업계획.md 2부 §2가 지정한 순서를 따랐다.
- **편성은 `game.state.partyIds`에 남긴다**: 전투 → 편성 복귀 시 그대로 이어지게. 기록이 없으면 스테이지 프리셋으로 채운다.
- **전투 중 메뉴는 씬을 바꾸지 않는다**: 씬을 전환하면 전투 상태가 날아간다. 오버레이로 띄우고 `update()` 전체를 멈춘다(연출·히트스톱 포함).
- **메뉴의 설정은 SettingsScene의 항목 정의를 빌려 쓴다**: `game.scenes.settings.items(game)`가 `game.settings`를 클로저로 잡고 있어 그대로 재사용된다. 설정 항목을 두 군데 정의하면 반드시 어긋난다.
- **개발 서버를 캐시 끄는 것으로 교체** (`tools/serve.py`): `python -m http.server`는 Cache-Control을 안 보내서 브라우저가 구 파일의 Last-Modified 기준 휴리스틱으로 ES 모듈을 길게 캐시한다. **코드를 고쳐도 옛 모듈이 도는** 이 문제로 이 세션에서만 세 번 시간을 날렸다(스프라이트·가호·편성창). 모든 응답에 no-store를 붙이고 조건부 요청 헤더를 제거한다. 게임실행.bat도 이 서버를 쓴다.
  - 주의: 이미 캐시된 항목은 새 헤더로 무효화되지 않는다. 서버 교체 직후 한 번은 Ctrl+Shift+R가 필요하다.
## 전투 중 ESC — 단계 뒤로가기 우선 (BETA, 회귀 수정)

- **ESC는 "한 단계 뒤로"가 우선이고, 뒤로 갈 곳이 없을 때만 일시정지 메뉴를 연다**: 대상 선택 → 스킬 선택 → 행동 선택 → 캐릭터 선택 해제 → [일시정지]. 잘못 고른 캐릭터를 되돌릴 방법이 없으면 그 턴을 통째로 날려야 한다.
- **내가 만든 회귀였다**: `cancelStep()`은 원래 있었는데, 일시정지 메뉴를 붙이면서(d1fae65) ESC를 switch 앞에서 가로채 단계 뒤로가기를 죽였다. 전역 단축키를 추가할 때는 그 키를 이미 쓰고 있는 단계가 있는지 먼저 확인할 것.
- **행동 패널에 「뒤로 [ESC]」 버튼을 넣었다**: 키보드에만 있으면 마우스 플레이어가 알 수 없다. 우클릭도 같은 동작(원래 있던 것).
## 전투 로직 감사 수정 10건 (BETA)

감사(4개 렌즈 병렬 검토 + 적대적 검증, 27 에이전트)에서 확정된 결함 전부 수정. 상세는 커밋 메시지.

- **상태 지속 보정은 저장값이 아니라 유예 플래그로**: 예전엔 기절·속박만 저장 턴수 +1로 보정했는데, 그러면 ① 나머지 디버프는 여전히 손해를 보고 ② 화면의 '남은 턴'이 스펙보다 1 크게 표시된다. **첫 감소 1회를 건너뛰는 `grace`** 로 바꿔 둘을 한 번에 해결했다. 상대 팀에 건 **비-도트** 상태에만 붙인다 — 도트는 감소 '전'에 발동하므로 유예를 주면 1회 더 터진다.
- **`applyStatus`는 `fromSide`를 받는다**: 상대가 건 것인지 판단해야 유예 여부가 정해진다. 호출부 전부에 전달했다.
- **재적용은 항상 `Math.max`**: 지속도 보호막도. 기획서의 "재적용 시 갱신"은 되살리는 의미이지 깎는 의미가 아니다. 아군의 이로운 행동이 아군에게 손해가 되면 그건 버그다.
- **예상 수치는 실제 실행과 같은 규칙을 써야 한다**: ☠는 협공 합산, HP 소모는 같은 클램프, 보호막은 같은 max. 저RNG 설계("모든 결과가 계산 가능")는 예상과 실제가 어긋나는 순간 무너진다.
- **광역기는 `primary`가 없다**: 치명 확률·상성을 primary 기준으로 계산하면 광역 공격 9종에서 전부 0이 된다. 첫 대상을 기준으로 삼는다.
- **처치 트리거는 막타를 넣은 유닛에게**: 협공·반격이 마무리한 경우 시전자가 아니라 그 협공자·반격자가 받아야 한다. computeDamage가 이미 협공을 협공자의 공격으로 취급하므로 코드 규약과도 맞는다.
- **감사가 밸런스보다 먼저인 이유가 증명됐다**: ①은 탱커 방어력을, ③은 디버프 스킬 하나를 통째로 무효화하고 있었다. 이 상태로 적 스탯을 조정했으면 전부 잘못된 기준 위에서 튜닝했을 것이다.
## 전투 하단 UI 배치 (BETA, 회귀 수정)

- **행동 버튼 줄과 토글 줄을 분리한다**: 오토·배속 토글이 행동 버튼과 같은 y(466)에 있어서, 「뒤로」를 추가하자 50px 겹쳤다. 토글을 턴 종료와 같은 윗줄(y418, h36)로 올려 아랫줄을 행동 버튼 4개가 통째로 쓰게 했다.
- **좌측 유닛 정보가 x197까지 차지한다** (패시브 라벨 포함). 행동 버튼은 206에서 시작한다.
- **버튼 폭은 글자 폭을 실측해서 정한다**: `font()`가 요청 크기를 정수배 티어로 스냅하므로 size 14는 실제 18px로 그려진다. "대기 [Space]"가 110px이라 폭 124로는 여유가 2px뿐이었다. 140으로 잡았다.
- **배치 변경은 기계적으로 검사한다**: 버튼끼리·토글과·턴 종료와의 사각형 겹침, 라벨 넘침, 패널 포함 여부를 전부 계산으로 확인한다. 눈으로 보면 놓친다 — 이번 겹침도 눈으로는 안 보였다(둘 다 같은 색 패널 위였다).## 전투 밸런스 1차 튜닝 (BETA, 작업계획 2부 §4)

- **"아군 최저 HP"는 전투 도중 최저점으로 잰다**: 종료 시점 HP로 재던 것을 바꿨다. 힐러가 마지막에 회복시키면 아슬아슬했던 판이 "여유로웠다"로 뒤집힌다 — 실측으로 보스가 세서 16턴 힐 공방이 벌어진 판이 최저 HP 57%(=너무 쉬움)로 읽혔다. 지표가 틀리면 그 위의 튜닝은 전부 헛수고다.
- **전투불능 아군은 최저 HP 지표에서 뺀다**: 0%를 섞으면 "누가 죽었다"와 "아슬아슬했다"가 한 숫자로 뭉개진다. 전투불능은 `deaths` 기준으로 따로 판정한다.
- **적 HP = 앵커 × 1.68**: 앵커(딜러 2행동 처치)는 1:1 기준이라 그대로 두면 4인 파티가 광역기·협공으로 5기를 2턴에 쓸어버린다. 다수전 보정을 곱해 일반 전투 3턴에 맞췄다.
- **역할 배수를 `{hp, atk}`로 쪼갰다**: HP만 올리면 "오래 걸리는데 안 아픈" 샌드백이 된다(HP만 ×11인 보스는 6턴이 걸려도 아군 최저 HP가 48%였다). 턴 수는 hp로, 위압감은 atk로 따로 맞춘다. 방어·속도는 올리지 않는다 — 공략거리가 아니라 벽이 된다.
- **보스 atk는 민감하다**: 1.05에서 최저 HP 24%(목표 20~40%)인데 1.1이면 17%로 뚫린다. 힐러가 힐 락에 걸리는 임계가 그 사이에 있다. atk를 만질 때는 반드시 재측정한다.
- **보스 호위는 2기**: 3기면 잡몹 정리에 턴을 다 쓰다 전투불능이 1명씩 나온다. 주술사를 남긴 건 "힐 우선 제거" 학습을 보스전에서도 잇기 위해서다.
- **버티기는 '이길 수 없는 적'이 아니라 '정리하기엔 벅찬 수'로**: 예전 구성(6기 Lv7~8)은 3~4턴에 아군이 전멸해 5턴을 볼 수조차 없었다. 4기 Lv2로 바꿔 5.7턴·전투불능 2명·최저 HP 41%가 됐다.
- **starTurns는 시뮬 평균 턴에 맞춘다**: 평균보다 후하면 ★3이 자동으로 붙어 별점이 보상 조건으로서 의미를 잃는다(선발대 6→3, 기사장 5→6).
- **조합 전수에서 두 가지 편차가 남았다**: ① 일반 전투에서 16%의 조합(지원가·탱커만 3명)이 승률 0%로 전멸한다 ② 보스전이 조합에 따라 36~38턴까지 늘어진다. 둘 다 '딜 하한' 문제라 적 킷 보강(§5)·육성 곡선(§6)에서 같이 본다. 지금 적 스탯으로 덮으면 정상 조합이 망가진다.
## 전투 진형 재배치 — 4번째 아군 HP바 가림 수정 (BETA, 사용자 보고)

- **진형의 세로 위치는 미관이 아니라 가림 제약이 정한다**: 유닛 정보(보호막·HP바·상태칩)는 발끝 기준 py+10~py+34에 그려지고, UI는 유닛보다 나중에 그려져 무조건 위를 덮는다. 가림판은 넷이다 — 하단 패널(y446) / 오토·배속·턴종료(y418, x566~942) / 턴 배너(y190~250) / 예상 결과 패널(y40~252, 실측 최대 7행). 앵커를 옮길 때는 이 넷을 **전부** 검사해야 한다.
- **화면 흔들림은 유닛에만 걸리고 UI에는 안 걸린다**: `render`의 `translate`가 `restore`보다 앞이고 패널은 그 뒤에 그려진다. 최대 세로 ±5.5px(SACRED.shake 0.55 × 10)를 여유로 더 빼야 한다. → 아군 최하단 ≤ 406, 토글 x대역에 걸치는 적 ≤ 378.
- **아군은 올리고, 적은 조인다**: 아군은 대각선 기울기를 그대로 둔 채 통째로 48px 올렸다(순수 평행이동 — 진형의 인상이 안 바뀐다). 적은 **올릴 수 없다** — 바로 위에 턴 배너와 예상 결과 패널이 붙어 있어 1px만 올려도 뒷줄 적의 HP바가 먹힌다. 그래서 세로 간격을 48→36으로 조였다. 설계안 4개 중 3개가 "적을 위로"를 시도했다가 여기서 죽었다.
- **돌진 예비 동작은 세로에 적용하면 안 된다**: `lungeCurve`는 예비 구간에서 음수(-0.12)를 돌려주는데, 이걸 `dy`에 그대로 곱하면 **위쪽 대상을 칠 때 시전자가 아래로 내려앉는다**. 최하단 아군이 최상단 적을 치면 py 396→411, 상태칩 바닥이 445가 되어 패널까지 1px만 남았다. `Math.max(0, f)`로 세로만 [자기 y, 대상 y] 안에 가둔다 — 가로 빼기(손맛)는 그대로다.
- **클릭 판정은 '앞에 있는' 유닛을 집는다**: 판정 상자(88×96)가 진형 간격보다 커서 이웃끼리 겹친다. 배열 순서대로 첫 번째를 돌려주면 뒤에 가려진 유닛이 잡혀 오조준이 난다. 발끝 y가 가장 큰 유닛 = 그리기 순서상 맨 위 = 플레이어 눈에 보이는 유닛을 집는다.
- **적5의 x를 875→862로**: 손그림 에셋(192폭)이 들어오면 875+96=971로 화면 밖이었다. 코드 생성본(128폭)일 때는 안 보이는 잠복 버그다.
- **눈이 아니라 계산으로 잡았다**: 기하 전수 검사(10앵커 × 6가지 가림판 × 정지+돌진 48조합 61스텝 × 흔들림 3단계)로 현행 277건 → 3건. 남은 3건은 최하단 아군↔3번째 적이 **돌진하는 중에** 흔들림이 겹칠 때 상태칩이 토글 버튼 위쪽을 최대 5.4px 스치는 것뿐이다(13px 칩 중, 몇 프레임). 이걸 없애려면 토글 줄을 또 옮겨야 해서 그대로 두었다.
## 전투 밸런스·원화를 육성 시스템 이후로 (사용자 결정 2026-08-03)

- **밸런스 튜닝과 적 킷 보강을 M5(육성 시스템) 뒤로 미룬다**: 지금 맞춘 수치는 *무장비·무돌·무각성* 전제다. 작업계획 2부 §4-2가 요구하는 육성 4축 기여 비중(레벨 50/장비 25/한돌 15/각성 10)은 장비 데이터가 없으면 역산 자체가 불가능하고, 장비가 들어오면 앞서 맞춘 적 스탯을 전부 다시 재야 한다. 두 번 일하지 않기 위해 한 번에 한다.
- **1차 튜닝 결과는 지우지 않고 기준선으로 남긴다**: 지금 플레이에 문제가 없고, 육성 시스템이 "무장비 상태에서 여기서 얼마나 강해지는가"의 출발점을 필요로 한다.
- **적 원화·적 이펙트를 원화 대기 묶음에 추가**: 아군만 손그림이 되면 적이 상대적으로 더 초라해 보인다. 화면 전체의 화풍은 같은 시점에 올려야 맞는다.
- **적 킷 설계 결과는 버리지 않고 보관**: 9개 에이전트 중 8개가 끝난 시점이라 죽이면 그 작업이 다 날아간다. 명세를 작업계획.md 부록으로 저장해 두고 재개 시 그대로 쓴다.
- **진행 순서는 계획서대로 M4 → M5**: 육성만 떼어 먼저 만들면 마일스톤 순서가 깨진다.

## M4 필드 — 화풍은 물을 필요가 없었다

- **기획서 §17 픽셀아트 스펙에 이미 정해져 있다**: 필드 타일 32×32, 캐릭터 스프라이트 32×32 × 4방향 × 걷기 2프레임, 코드 생성. 맵은 지역당 2개, 약 40×30타일.
- 사용자에게 물어보려던 "필드 캐릭터 화풍(2등신 vs 전투와 동일)" 질문은 **기획서를 먼저 읽었으면 나오지 않았을 질문**이다. 미정 사항으로 단정하기 전에 기준 문서부터 검색할 것.
## 밸런스 검증은 '주인공 3직업 × 난이도 3단계' 격자로 해야 한다 (2026-08-03, 내 실수 정정)

- **1차 튜닝은 9칸 중 1칸만 측정한 것이었다**: `judgeAll(n)`을 인자 없이 돌리면 `createBattleFromStage`의 기본값 `heroClass='sword'`가 쓰인다. "전 스테이지 목표 범위 진입"이라고 보고했지만 **주인공이 검사일 때만** 성립했다. 마법사·궁수는 노말에서 4스테이지 전부 불합격이고, 망령 기사장전이 검사 6.56턴 대비 14~17턴까지 늘어진다.
- **기본값은 검증에서 가장 위험한 것이다**: 함수가 조용히 채워주는 값은 "측정하지 않은 축"이 되어 결과에 보이지 않는다. 앞으로 시뮬 판정은 **격자로 돌리고, 격자의 각 축을 명시적으로 넘긴다.**
- **합격 기준에 난이도 보정이 빠져 있었다**: 작업계획 2부 §5는 "이지 +15%p / 하드 −15%p"를 정했는데 `PASS_CRITERIA`는 노말 기준 하나뿐이다. 그래서 하드가 전부 불합격으로 나오는데, 이건 적이 센 게 아니라 **잣대가 없는 것**이다. 판정 불가와 불합격을 구분하지 않으면 없는 문제를 고치게 된다.
- **이 발견은 적 킷 검증에서 부수적으로 나왔다**: 적 킷을 검증하던 에이전트가 "기준선 자체가 mage/archer에서 스펙 밖"이라고 지적했고, 직접 재보니 사실이었다. 적 킷을 얹기 전에 기준선부터 고쳐야 한다는 순서 문제도 같이 드러났다.
## M4 필드 탐험 (BETA)

- **맵은 문자 격자로 쓴다**: 한 글자 = 한 타일이라 소스에서 지형이 그대로 보이고, 맵 추가가 코드 수정 없이 문자열 추가로 끝난다(절대 규칙 3). 대신 **손으로 쓴 격자는 한 칸 어긋나도 눈에 안 보인다** — 실제로 첫 작성에서 안쪽 56행을 전부 한 칸씩 길게 썼다. 그래서 `validateMaps()`가 행 길이·타일 문자·오브젝트 위치·배치 규격을 로드 시 전부 검사한다.
- **도달성은 너비 우선 탐색으로 검사한다**: "갈 수 없는 곳에 상자를 둔" 사고는 눈으로 절대 안 보인다. 스폰에서 BFS를 돌려 ① 모든 오브젝트가 걸어서 닿는지 ② **숨김 상자는 숨김길을 통과해야만 닿는지**(숨김길을 벽으로 본 BFS에서는 안 닿아야 한다)를 확인한다. 후자가 없으면 "숨겼다"는 게 말뿐이 된다.
- **숨김길은 `solid: false`다**: 막아 두면 "밟아서 발견"이 성립하지 않는다. 위장은 렌더에서만 한다 — 발견 전에는 주변 바위와 똑같이 그린다.
- **맵은 통짜로 굽는다**: 1280×960 오프스크린 한 장에 미리 깔고, 매 프레임은 카메라 사각형만 잘라 오는 `drawImage` 1회다(절대 규칙 7). 실측 평균 0.197ms. 예외는 물결과 드러난 숨김길뿐 — 상태가 변하거나 움직이므로 위에 덧그린다(화면에 보이는 물 타일 최대 40장).
- **타일 프리렌더는 로드 때 미리 데운다**: 첫 필드 진입에서 만들면 591ms 멈춘다. 스프라이트 에셋과 같은 이유로 게임 시작 시 굽는다(두 번째 맵은 3.2ms — 타일 변형이 캐시되어서다).
- **필드 스프라이트는 직접 픽셀로 그린다**: 전투(64×64)는 고해상도 벡터→축소→양자화가 유리하지만, 32×32는 인물이 22px라 축소하면 이목구비가 뭉개진다. `spriteCfg`는 전투와 공유해 같은 인물로 보이게 한다.
- **눈으로 안 보이는 그림 버그는 픽셀을 세서 잡는다**: 풀 타일이 1024px 중 959px 단색이었고(사실상 통짜 초록), 나무는 수관이 기둥을 완전히 덮어 **갈색 픽셀이 0개**였다. 둘 다 색 분포를 세서 발견했다. 또 수관 안쪽을 풀과 같은 색으로 칠하면 나무가 배경에 녹는다 — 바깥을 더 어둡게 잡아야 실루엣이 선다.
- **심볼은 플레이어보다 느리다** (추격 110 vs 이동 140): 그래야 기획서 §13의 "회피 가능"이 성립한다. 추격 중에는 머리 위에 느낌표를 띄워 들켰다는 걸 즉시 알린다 — 모르면 피할 기회가 없다.
- **전투 복귀는 맵을 지정하지 않는다**: 지정하면 `field.enter`가 스폰 지점으로 되돌려 전투 직전 서 있던 자리를 잃는다. 세이브에 남은 좌표를 그대로 쓰게 둔다. 승리는 필드 복귀(심볼 제거), 패배는 거점 — 지는 데 대가는 있되 게임오버 화면은 두지 않는다.
- **읽는 곳 없는 파라미터는 넣지 않는다**: 기습(뒤에서 닿으면 선공)은 전투 로직이 '적 페이즈로 시작'을 지원해야 성립한다. M4 범위 밖이라 판정만 남기지 않고 아예 뺐다 — 남겨 두면 나중에 "이미 되는 줄" 착각한다.
- **리젠 시계는 실시간이 아니라 누적 플레이 시간**: 저장하고 껐다 켜도 일관되고, 세이브에 그대로 실린다.
- **입력에 눌림 상태를 추가했다**: 필드 이동은 "누르고 있는 동안" 동작해야 하는데 기존 Input은 이벤트만 있었다. `keys`/`mouseHeld`를 더하고, **창을 벗어나면(blur) 전부 턴다** — 안 그러면 Alt+Tab 한 순간의 키가 남아 캐릭터가 혼자 걸어간다.
## 심볼 인카운터를 '구간' 방식으로 (사용자 결정 2026-08-03)

- **문제는 추격이 아니라 배치 밀도였다**: "조금만 움직이면 전투"라는 피드백에 처음엔 추격 로직(시야각·추격 포기·전투 후 유예)을 손봤는데, 같은 경로를 걸어보니 전투 횟수가 그대로였다. 재보니 **맵의 46~55%가 심볼 감지 범위**였다. 기획서 챕터 표준의 "심볼 맵당 5~7"을 따랐지만 실제 통행 면적이 900타일 남짓이라 밀도가 과했다. **증상을 보고 원인을 짐작하지 말고 측정할 것.**
- **위험 구간(dangerZones)을 맵 데이터에 명시한다**: 심볼은 지정된 사각 구간 안에서만 나오고 **구간 밖으로는 쫓아 나오지 않는다**. 길·다리·제단 공터는 어느 구간에도 안 들어가므로 요새에서 다음 맵까지 싸우지 않고 갈 수 있다. 싸우고 싶을 때만 구간에 들어간다. 결과: 위험권 46%→27% / 55%→34%, **길로 가로지르기 전투 0회**.
- **구간은 보여야 한다**: 옅은 어둠 + 점선 테두리로 그리고 HUD에 현재 구간 이름("안전 지대" / "⚠ 북쪽 초원")을 띄운다. 안 보이면 "피할 수 있다"가 성립하지 않는다.
- **검증기가 설계를 지킨다**: 심볼이 구간 안에 있는지, 출구·제단·NPC가 구간 밖인지, **길·다리 타일이 통째로 구간 밖인지**를 로드 시 검사한다. 이게 없으면 나중에 맵을 늘릴 때 "안전한 길"이 조용히 사라진다. 실제로 이 검사가 NPC 하나가 구간에 들어간 걸 잡았다.
- 곁들여 고친 추격 로직(유지): 정면 시야각 150°만 감지(등 뒤로 돌면 못 본다) / 4.5초 넘으면 추격 포기 / **전투 직후 3초는 아무도 안 쫓는다**(복귀 지점에 심볼이 붙어 있으면 전투가 연달아 터진다) / 리젠 90→420초.

## 에셋 조달 규칙 개정 — 자체 작화 + 외부 에셋 혼합 (사용자 결정 2026-08-03)

- **코드 생성 픽셀아트를 최종본으로 쓰지 않는다**: 결과물이 기대에 못 미친다는 사용자 판단. 절대 규칙 1을 고쳤다 — 자체 작화 범위를 **전투/필드 스프라이트·타일**까지 넓히고, 외부 에셋 허용을 **BGM에서 타일셋·스프라이트·UI 팩까지** 확대했다.
- **코드 생성이 최종본인 영역은 이펙트·효과음만 남는다**: 나머지는 그림이 들어오기 전까지의 임시 대체다.
- **"그림체 불일치가 균일한 소박함보다 나쁘다"는 원칙은 살아 있다**: 외부 에셋을 허용했다고 아무거나 쓰는 게 아니라, 팔레트·해상도·화풍이 붙는 것만 고른다. 상업 이용 가능 라이선스만, CC-BY는 CREDITS.md에 기록.
- **교체 비용을 낮게 유지하는 게 이 개정의 전제다**: 스프라이트는 이미 `assetKey`가 있으면 그림을 쓰고 없으면 코드 생성본을 쓰는 구조다(전투). 타일도 `tiles.js`의 그리기 함수 한 곳만 이미지 로드로 바꾸면 되게 되어 있다. 이 구조를 깨지 말 것.
## 외부 에셋 도입 — Ninja Adventure Asset Pack (BETA, 사용자 승인 2026-08-03)

- **채택**: Ninja Adventure Asset Pack (Pixel-Boy & AAA). 팩 동봉 LICENSE.txt와 README에서 **CC0 1.0** 확인 — README 원문 "even commercial ones". 스팀 출시 목표에 걸림돌이 없다. 크레딧은 의무가 아니지만 `CREDITS.md`에 남겼다.
- **89MB 중 146KB만 저장소에 넣는다**: 필드에 걷는 건 주인공 1명 + 적 5종뿐이고 타일은 5장이면 된다. 팩 전체를 커밋하면 저장소가 감당이 안 된다.
- **셀은 눈이 아니라 측정으로 골랐다**: ① 완전히 꽉 찬 셀만 후보 ② 좌우·상하 가장자리 색차(seam)가 0인 것 = 반복해 깔아도 이음새가 안 보이는 타일 ③ 색 계열 분류. 화면을 볼 수 없는 상태에서 "예뻐 보이는 것"을 고를 방법은 이것뿐이었다.
- **단, seam만 최적화하면 가장 밋밋한 셀이 뽑힌다**: 첫 선택에서 풀·길이 1024px 중 100% 단색으로 나왔다. 이 팩의 바닥 타일은 원래 평평하고 디테일은 별도 오버레이로 얹는 설계다. 그래서 **타일 자신의 평균색에서 뽑은 밝고 어두운 점을 흩뿌리는 speckle**을 넣었다 — 새 색을 안 들여오므로 원본 화풍이 안 깨진다.
- **팔레트 32색 스냅은 하지 않는다**: 처음엔 외부 그림도 32색으로 스냅했는데, 실측 결과 풀·길·물이 전부 한 색으로 뭉개져 잔디 결이 통째로 사라졌다(색수 1). **좋은 그림을 가져와서 도로 뭉개는 셈**이다. 32색은 코드 생성물과 UI의 통일 규격이지 외부 그림에까지 강제할 것이 아니다. 대신 명도 −14%·채도 −10%로 **톤만** 맞춰 "함락된 왕국" 분위기와 남색·금색 UI에 붙였다.
- **스프라이트 시트 규격은 실측으로 확정**: 64×64가 4방향×4프레임인 건 알았지만 행/열 중 무엇이 방향인지는 문서에 없었다. 픽셀 비교로 **행 0과 행 2가 100% 동일**(= 걷기 중립 자세)임을 확인해 **열 = 방향**으로 확정했고, 해골 스프라이트가 **정면에만 눈구멍**이 있어 열0=아래·열1=위를 확정했다. 열2·열3은 완전한 거울이라 좌우는 분석으로 못 가린다 — 관례를 따르고 `SIDE_LEFT/SIDE_RIGHT` 두 숫자로 뒤집을 수 있게 두었다.
- **필드 인카운터는 `kind`로 유형을 명시한다**: 시뮬레이터가 적 수만 보고 판정하면 필드 잡몹(2턴이 정상)이 스토리 전투 기준(3~4턴)에 걸려 영영 불합격이다. 데이터가 유형을 선언하면 그걸 따르게 했다. 프리셋 파티도 실제 플레이와 같은 4인으로 맞췄다 — 시뮬 전용이라도 실제와 다르면 측정값이 거짓이 된다.
- **스프라이트 좌우는 얼굴 위치로 확정했다**: 옆모습 두 열이 완전한 거울이라 형태로는 못 가렸는데, 마법사 스프라이트의 피부색 rgb(242,173,125)이 열2에서 칸 중앙 기준 −3.95px(왼쪽), 열3에서 +3.95px(오른쪽)에 있어 열2 = 왼쪽으로 확정했다. **화면을 볼 수 없을 때는 "무엇이 어디에 있는지"를 픽셀로 물어보면 답이 나온다.**
## 필드 그래픽 재교체 — 정통 JRPG 화풍 (사용자 결정 2026-08-03)

- **Ninja Adventure(CC0)를 버리고 Ivan Voirol + Eldiran으로 갈아탔다**: 사용자가 "화풍이 마음에 들지 않는다"고 해서 방향을 물었고 "정통 JRPG 느낌"을 골랐다. 교체 지점(`tile_assets.js` / `field_sprite_assets.js`)을 미리 만들어 둔 덕에 **좌표 데이터만 바꿔서** 끝났다 — 렌더 코드는 손대지 않았다.
- **픽셀 밀도를 맞추는 게 화풍 통일의 핵심이다**: Ninja 팩은 16×16을 2배 확대한 것이라, 네이티브 32×32 타일과 섞으면 인물만 뭉툭해 보인다. 이건 색이나 톤보다 훨씬 눈에 띄는 불일치다. 그래서 캐릭터도 **네이티브 32×32**인 Eldiran 팩으로 맞췄다.
- **마젠타 키(255,0,255) 지원을 넣었다**: 고전 픽셀아트 시트가 투명도 대신 쓰는 방식. `toneCanvas`에 `colorKey`를 더했다.
- **좌우 반전 지원을 넣었다**: Eldiran 시트는 옆모습이 한쪽뿐이라 반대쪽을 뒤집어 만든다. 정수 좌표 반전이라 픽셀이 안 뭉개진다.
- **시트 배치는 또 실측으로 확정했다**: 12열×21행에서 **행 = 캐릭터, 열 0~3 아래 / 4~7 위 / 8~11 옆모습**. 판정 근거는 ① 상반부 눈 두 개 검출(정면만) ② bbox 폭 26px→16px(옆모습). 옆모습은 대부분 3프레임이라 [8,9,10,9]로 왕복시킨다. **21행 중 3방향이 다 있는 건 11행뿐**이라 그 안에서 8종을 골랐다.
- **CC-BY는 게임 안 표기가 의무다**: Ivan Voirol 두 팩이 CC-BY라 `CREDITS.md`만으로는 부족하다. 크레딧 화면을 M6까지 반드시 만들어야 하고, 그 전에는 배포하지 않는다고 CREDITS.md 맨 위에 못박았다. Basic map은 CC-BY 3.0/GPL 다중 라이선스라 **CC-BY 3.0을 선택했음을 명시**해 GPL 해석 여지를 닫았다.
## 필드 캐릭터를 전투 원화 수준으로 (사용자 요청 2026-08-03)

- **32×32 픽셀아트로는 그 밀도가 안 나온다**: 사용자가 "주인공 전투 원화 정도의 퀄리티가 움직이는 것"을 원했다. 32×32 안에서는 인물이 22px 남짓이라 이목구비·장비 디테일을 담을 픽셀이 물리적으로 모자란다. 그래서 **크기 제한을 푸는 쪽**으로 갔다.
- **전투와 같은 낱장 원화 경로를 뚫었다**: 시트에서 잘라 쓰는 대신 프레임마다 PNG 한 장(`files`). 전투가 이미 이 방식이고(`sprite_assets.js`), 원화를 한 장씩 그려 나가는 작업 흐름과 맞다. 크기·기준선도 전투와 동일(96×64, 발끝 y=62)하게 잡아 **변환 도구를 두 벌로 만들지 않았다.**
- **발끝 기준으로 그린다**: 32×32 픽셀 스프라이트든 96×64 손그림이든 같은 규칙으로 서게 했다. 발 위치가 흔들리면 걷는 게 부자연스러워진다. `drawUnitSprite`가 이걸 한 곳에서 처리한다.
- **반쪽만 원화면 채택하지 않는다**: 네 방향이 전부 있어야 손그림 경로를 쓰고, 하나라도 없으면 통째로 픽셀 스프라이트로 떨어진다. 모퉁이를 돌 때마다 화풍이 바뀌면 안 하느니만 못하다.
- **주인공은 옆모습 원화만 있다**: 전투가 사이드뷰라 그것만 그렸다. 지금은 임시로 정면·뒷모습에도 옆모습을 돌려 쓴다 — 걷는 크기·퀄리티를 먼저 확인받기 위해서다. 필요한 그림(정면 4 + 뒷모습 4)은 `작화_규격서.md` 4-3부에 정리했다.
- **필드에서 걷는 건 주인공 1명 + 적 심볼 5종뿐이다**: 동료 15명은 필드에 안 나온다. 그래서 우선순위 1번은 **주인공 8장**이고, 그것만으로도 화면 인상이 크게 바뀐다.
- **원화 생성은 내가 못 한다 — 프롬프트로 넘긴다**: 사용자가 "주인공 화풍으로 직접 생성해 달라"고 했으나 이미지 생성 도구가 없다. 대신 주인공 원화(`_source/idle0.png`, `walk1.png`)를 **직접 열어 보고** 화풍을 실측해 `원화_생성_프롬프트.md`를 만들었다 — 금·감청·흑철 배색, 4등신, 진한 검정 외곽선, 순백 배경, 옆모습. 주인공을 같은 방식(AI 생성 → convert_sprite.ps1)으로 만들었으므로 그대로 이어서 쓸 수 있다.
- **못 하는 것은 못 한다고 하고, 할 수 있는 것으로 최대한 좁혀 준다**: 프롬프트를 캐릭터별로 쓰고, 프레임별 동작표·네거티브 프롬프트·변환 명령·검수 체크리스트까지 붙였다. 받은 그림을 넣을 자리(`handDrawn()`)도 미리 만들어 뒀다.
## 필드 코드 생성본을 전투 생성기로 통일 (2026-08-03)

- **이미 있던 걸 안 쓰고 있었다**: `makeUnitSprites`(전투 스프라이트 생성기)가 **이미 `idle`에 4방향(down/up/left/right)을 64×64로** 만들어 두고 있었다. 그런데 필드는 내가 따로 쓴 32×32 직접 픽셀 그리기를 쓰고 있었다. 전투 쪽은 고해상도(4배) 벡터 곡선 → 면적평균 축소 → 팔레트 양자화 → 외곽선 파이프라인이라 망토가 휘고 어깨가 둥글게 나온다 — 비교가 안 된다. **새로 만들기 전에 이미 있는 것부터 찾을 것.**
- **필드 코드 생성본을 전투 생성기로 갈아탔다**: 크기가 32×32 → 64×64로 두 배가 되어 주인공 손그림(96×64, 인물 58px)과 크기가 맞고, **같은 생성기라 필드↔전투 화풍이 저절로 일치한다.** 실측: 정면 783px·색 9~14 (이전 570px·색 13).
- **적을 Eldiran 32×32 시트에서 뺐다**: 사용자가 "다른 몹이나 NPC도 그 정도 크기랑 퀄"을 원했는데, 32×32는 주인공 원화의 절반 크기라 같은 화면에 서면 안 맞는다. 등록표에서 빼면 자동으로 64×64 생성기로 떨어진다. 시트 경로(`row()`)는 지우지 않았다 — 되돌릴 일이 생기면 한 줄로 복구된다.
- **이미지 생성은 여전히 못 한다**: 코드로 그리는 것과 AI로 원화를 뽑는 것은 다르다. 원화가 필요한 부분은 `원화_생성_프롬프트.md`로 넘겼고, 그때까지는 이 64×64 생성본이 대역을 한다.
- **원화는 방향당 1장만 받는다**: 걷기 프레임을 여러 장 일관되게 뽑는 건 AI로 어렵고 비싸다. 그래서 **서 있는 그림 한 장**만 받고 두 번째 프레임을 코드로 만든다(`bobFrame`) — 몸을 1px 내리고 아래쪽(다리)만 1px 눌러 무게가 실리는 순간을 흉내 낸다. 32px 타일 위에서는 그것만으로 걷는 것으로 읽힌다. 실측: 프레임 간 1003px이 달라진다. **프레임을 여러 장 주면 보정이 자동으로 꺼지고 그쪽을 쓴다.** 요구 장수를 8장에서 2장으로 줄인 게 핵심이다.
- **serve.py가 한글 콘솔에서 죽고 있었다**: 시작 메시지의 em dash(—) 하나 때문에 cp949 인코딩 오류로 서버가 뜨지도 못했다. 출력 인코딩을 UTF-8로 재설정하고 실패 시 ASCII로 떨어뜨리게 고쳤다. 포트 충돌 안내도 붙였다. **개발 도구가 조용히 죽으면 "왜 안 되지"로 시간을 날린다.**
- **주인공 정면·뒷모습 원화 도착 (2026-08-05)**: 1254×1254 원본을 `-CharHeight 58`로 변환해 96×64에 넣었다. 기존 옆모습은 512×768에 인물 350px, 새 원화는 1254×1254에 990/968px로 **캔버스도 인물 크기도 달랐다** — 그래서 고정 배율(`-Scale`) 대신 목표 높이 지정을 썼다. 한 세트 안에서 배율을 고정하라는 원칙은 *같은 캔버스에서 나온 프레임들* 얘기고, 출처가 다른 그림은 **최종 높이**를 맞추는 게 맞다. 결과: 발끝 y61/61/60/60으로 방향을 바꿔도 안 튄다.
- **걷기 프레임은 시간이 아니라 이동 거리로 넘긴다 (2026-08-05)**: 사용자가 "슬라이딩 같다"고 했을 때 원인이 둘이었다. ① 정면·뒷모습에 다리 움직임이 없는 것(원화 부족) ② **애니메이션이 초당 7프레임 고정이라 발 딛는 리듬과 실제 이동 속도가 어긋나는 것**. ②는 프레임을 아무리 늘려도 안 없어진다. `walkFrame(dist, n)`으로 바꿔서 **한 사이클에 나아가는 거리(64px)를 고정**했다 — 원화를 4장에서 16장으로 늘려도 보폭이 그대로 유지된다. 덤으로 **벽에 막히면 발도 멈춘다**(실측 0.00px) — 예전엔 벽에 붙어서도 계속 걷는 시늉을 했다.
- **애니메이션 문제를 원화 부족으로만 진단하면 안 된다**: 그림을 더 받아도 안 고쳐질 문제였다. 원화를 요청하기 전에 재생 쪽을 먼저 봤어야 했다.
## 주인공 걷기 16프레임 반영 (2026-08-05)

- **캡션이 그림에 구워져 있었다**: 받은 16장 중 12장에 파일명이 회색 글자로 찍혀 있었다. 그냥 변환하면 bbox 아래끝이 글자가 되어 **캐릭터가 공중에 뜨고 글자까지 스프라이트에 들어간다.** 아래에서 올라오며 만나는 첫 잉크 덩어리가 '무채색만'이면 캡션으로 보고 지우게 했다(`load_ink`). 실측으로 안 봤으면 못 잡았다.
- **가로 정렬 기준을 발에서 머리로 바꿨다**: PS 변환기(`convert_sprite.ps1`)는 아래 16% 무게중심(=발)을 캔버스 중앙에 둔다. 서 있는 포즈 세트에는 맞지만 **걷기 8프레임은 다리가 크게 벌어져 발 중심이 원본 기준 48px 움직인다** — 그대로 쓰면 몸통이 좌우로 덜덜 떨린다. 걷기에서 가장 안 흔들리는 건 머리다. 그래서 이 세트만 전용 빌더(`tools/build_hero_walk16.py`)를 따로 만들었다.
- **★검이 4프레임마다 손을 바꾸고 있었다**: 원화 AI가 후반 사이클(4~7)을 **캐릭터째 좌우 반전**해서 그렸다. 실측 — 정면 검 위치(머리 중심 대비) 0~3 = -21/-10/-19/-13, 4~7 = +23/+14/+13/+13. 58px 크기에서도 0.23초마다 검이 반대쪽으로 순간이동하는 게 뚜렷하게 보인다. 기준은 전투 옆모습(왼쪽을 보며 검을 몸 앞 = **캐릭터의 왼손**)이므로 4~7이 맞고 0~3이 틀렸다 → **0~3을 반전해서 쓴다.** 반전 후 실측: 정면 전 프레임 +1.2~+6.8, 뒷모습 -0.0~-3.3 (한쪽 고정).
- **반전하면 선행발 교대가 사라지지만 그게 낫다**: 좌우 반전은 검과 다리를 같이 뒤집으므로 "왼발이 먼저 나가는 반 사이클"이 두 번 도는 셈이 된다. 그래도 **몸의 상하 물결은 한 사이클에 두 번**(한 걸음에 한 번) 그대로 남고, 정면·뒷모습에서 어느 발이 앞인지는 망토에 가려 거의 안 보인다. 검이 튀는 쪽이 비교가 안 되게 눈에 띈다. — *다음에 원화를 뽑을 땐 "4~7은 반전하지 말고 검을 화면 오른쪽에 그대로 둘 것"을 명시한다.*
- **방향별로 다른 배율을 썼다**: 정면 인물 225px / 뒷모습 220px로 원화 크기가 달랐다. 같은 배율을 쓰면 **뒤돌 때 캐릭터가 커진다.** 방향마다 목표 높이 58px에 맞췄다. "한 세트는 배율 고정"은 *같은 캔버스에서 나온 프레임끼리* 얘기다.
- **멈췄을 때 쓸 프레임을 따로 지정했다(`idleFrame`)**: 0번은 다리가 가장 벌어진 접지 순간이라 그대로 멈추면 걸음을 얼어붙인 것처럼 보인다. 두 다리가 모이는 2번을 선 자세로 쓴다.
- **footY를 62 → 61로 고쳤다**: 변환본의 마지막 잉크 행이 60인데 footY가 62라 캐릭터가 1~2px 떠 있었다. 옆모습(전투 원화)도 같은 문제였다. 실측 결과 16프레임 전부 화면 y=285에 발이 닿고 지면선이 286이다.
## 필드 배경 전면 재선정 — 타일 셀을 눈으로 보고 다시 골랐다 (2026-08-05)

- **사용자: "배경이 조잡하다" — 원인은 등록된 셀이 전부 엉뚱한 것이었다**: 밭이랑을 잔디로, 실내 벽+문짝을 길로, 지붕 모서리를 나무로, 창살 문을 절벽으로 쓰고 있었다. 이전 선정이 seam·색수 같은 **통계만으로** 골랐기 때문이다 — 통계는 "반복해도 이음새가 없다"만 보장하지 "이게 잔디인가"는 보장하지 못한다. 이번에는 두 시트 전체를 격자·좌표 라벨을 붙여 렌더링해 **눈으로 확인**하고 골랐다. **캔버스 캡처를 파일로 빼내는 경로(브라우저 toDataURL → 툴 결과 파일 → 디코딩)가 뚫려서 가능해진 일이다. 이제 화면 검증에 스크린샷 실측을 쓴다.**
- **잔디는 (1,2) 한 셀만 쓴다**: 이 팩은 셀마다 가장자리가 살짝 어둡다(실측 seam -4~-22, (1,2)만 +1.1). 셀을 여러 개 섞으면 그 가장자리들이 격자로 드러난다 — 화면의 조각보 무늬가 그것이었다. seam 0인 한 셀만 깔면 격자가 완전히 사라진다. 변형은 스페클(타일 평균색 점)로 만든다 — 새 색이 안 들어와 화풍이 유지된다.
- **길은 흙이 아니라 석판**: 흙(밝기 55)은 잔디(78) 옆에서 검은 띠로 보였고, 모래(198)는 사막처럼 쨍했다. 석판(131~144)이 밝기가 조화롭고, "함락된 왕국의 옛 가도"라는 서사에도 맞는다. 셋을 잔디 위에 깔아 비교해 정했다.
- **숲은 2×2 수풀 퀼트**: 큰 수풀 2×2를 (x%2,y%2) 좌표 패리티로 이어 깔면 군집이 한 덩어리 숲으로 보인다 (mapCanvas에 TILE_PATTERNS 지원 추가). 랜덤으로 고르면 조각이 어긋나 조각보가 된다. 사방에 숲이 없는 단독 나무 칸은 작은 수풀 한 그루로 떨어진다. 네 조각이 전부 잎이라 군집 가장자리 어떤 모양에서도 안 깨진다.
- **물은 같은 자리의 애니메이션 페어**: 이 팩의 물 구역은 PART 1↔PART 2(열 +10)가 같은 그림의 물결 프레임이다(실측 620px 차이). 이전엔 서로 다른 무늬 3개를 돌려서 물결이 아니라 그림 점프로 보였다. 밝은 페어를 쓴 이유: 다리 판자 틈에 밝은 물이 구워져 있어 어두운 물을 깔면 다리 주변만 색이 튄다.
- **base도 그림으로 깐다**: 나무·바위·꽃의 배경(base)을 코드 생성 잔디로 깔면 오브젝트 주변만 배경이 달라져 사각 얼룩이 생긴다. cutTileArt가 base도 등록표의 그림 잔디로 깔도록 고쳤다.
- **절벽 = 이끼 낀 돌담**: 맵 경계가 폐허 성벽으로 읽힌다. 톤 보정은 darken 0.08 / desaturate 0.12로 올렸다(원본이 쨍하다).
- **저작자 표기 재확인**: basicmap 시트 안 서명이 `by SILVER IV`라 확인했더니 OGA 페이지 공식 Attribution은 `Ivan Voirol`(Silver IV는 같은 작가의 별칭). CREDITS.md에 기록.
- **걷기 어색함 실측 진단 (2026-08-05, 사용자: "좌우·북쪽이 어색")**: 옆모습 = 전투 원화 유용의 한계 — 2번 프레임에서 머리가 18px 옆으로 점프(돌진 자세), 높이 8px 출렁. 뒷모습 = AI가 4~7을 캐릭터째 반전 생성한 것을 되뒤집은 부작용 — 좌우 교대가 사라져 망토·칼자루가 사이클마다 같은 쪽으로 두 번 쏠림(절뚝임). 둘 다 코드로는 못 고친다 → 옆모습 8장 신규 + 정면·뒷모습 재뽑기 24장 발주로 정리(원화_생성_프롬프트.md). 필드 전용 옆모습은 검을 뽑지 않은 평보 자세로 — 전투 원화는 전투에 유지.
- **launch.json을 원본 기준으로 통일 (이식 준비)**: BETA 절대 경로가 박혀 있으면 이식 후 원본이 BETA 서버를 띄운다. 이식 전에 원본 경로·포트 3010으로 바꿔 커밋해 fast-forward 한 번으로 끝나게 했다.
## 마법 이펙트 고퀄화 (2026-08-05, 사용자 지시로 원화 대기 묶음에서 분리)

- **burst·ring·rain을 참격(SLASH)의 시각 언어로 통일**: 합성 'lighter' + [후광 → 몸통 → 흰 심] 3겹. 단색 사각형만 날리던 이전과 달리 겹칠수록 밝아져 중심이 하얗게 타오른다. 수치는 fx.js의 MAGIC에 모았다 (연출≠밸런스 분리 원칙).
- **shape:'ring' 렌더러 자체를 3겹 발광으로 올렸다**: 성물 해방·방패·착탄 등 링을 쓰는 모든 곳이 한 번에 좋아진다. 개별 스폰 코드를 고치는 것보다 렌더러를 고치는 쪽이 파급이 크다.
- **rain에 인과를 넣었다**: 이전엔 사각형이 허공에서 사라져 "떨어졌다"는 결과가 없었다. 낙하 줄기(흰 머리+색 꼬리)가 시간차로 꽂히고 닿는 자리마다 착지 섬광이 터진다. 가운데부터 바깥으로 번갈아 배치 — 왼쪽부터 차례로 오면 커튼처럼 보인다.
- **시간차 파티클의 이동 버그를 같이 잡았다**: life<0(시작 전) 파티클도 vx/vy로 움직이고 있었다. 낙하 방울이 대기 중에 미리 떨어져 착지 섬광과 어긋나는 문제 — update 루프에서 life<0이면 건너뛴다.
## 전투 로직·AI 보충 감사 (2026-08-05, 작업계획 선행 작업 이행)

다각 검토(데미지·턴·AI·승리조건·RNG) → 반박 검증으로 걸렀다. 발견 11건 중 실제 6건.
**목적이 "로직 버그가 밸런스 문제로 위장되는 것을 막는다"이므로, 수치가 바뀌지 않는 것만 즉시 고쳤다.**

즉시 수정 (밸런스 무영향 4건):
- **죽은 밸런스 노브 2개 삭제**: assist.markMult(실제는 status.mark.assistMult만 읽음), auto.seraphinSelfHpGuard(실제는 passives.c03_special.autoSkipCostHpPct만 읽음). 튜너가 이걸 돌려도 시뮬이 안 변하는 함정 — 자리에 단일 출처 안내 주석을 남겼다. 절대 규칙 2의 목적(한 곳만 고치면 반영)이 깨져 있던 지점.
- **협공·반격 예상에 이번 스킬의 디버프를 가적용**: 실행 순서가 본타→디버프→협공·반격이라, 표식 스킬의 협공 예상이 최대 2배 과소, 공격약화 스킬의 반격 예상이 과대였다. statuses 배열만 복제→계산→원복. 검증: 예상 5 = 실행 5 일치, 원복 확인. 덤으로 기절 스킬은 반격 예상이 자연히 사라진다.
- **버티기 별점 오프바이원**: 버티기 승리는 turn++ 직후 판정이라 -1 보정이 있는데, victory.type으로 갈라서 **전멸로 이겨도** 할인을 받았다. wonBySurvive 플래그로 승리 경로를 기록. 검증: 6턴 전멸 2별(구 3별), 6턴 버티기 3별.

밸런스 재개로 이관 (수치가 바뀌는 설계 판단 2건 — 작업계획 §6 재개 목록에 추가):
- 반격의 postDefCoef 전환 여부 (협공과 비대칭 — 물리 반격이 최소보장으로 주저앉음)
- 표식·방어약화의 grace 유예 적정성 (거는 쪽 턴에 효력을 내는 디버프는 실효 3턴이 됨)

버그 아님 판정: computeEnemyStats 이중 반올림(현행 튜닝이 흡수 — 확정 사양), 시뮬 기본 경로 검사 편중(이미 §6에 문서화된 재개 항목).

교훈: **감사 서브에이전트가 사용량 한도로 죽으면 "기각"과 "미검증"을 구분해야 한다.** 워크플로 결과의 rejected 10건 중 9건이 실은 검증 미실행이었고, 본 세션에서 직접 코드를 읽어 판정을 마쳤다 (ai.js·승리조건 관점도 직접 이행 — 이지 연계 금지는 logic.js 378행에 실재).
## 투사체 고퀄화 — 프리렌더 스프라이트 방식 (2026-08-05, 사용자 레퍼런스 8장)

- **"픽셀이 부족해서"가 아니라 "그리질 않아서"였다**: 이전 투사체는 사각형 줄무늬 16px. 사용자 레퍼런스(픽셀 스킬 이펙트 시트의 혜성형 화구)를 기준으로, 40×22 아트 픽셀을 **2배 블록(80×44)** 으로 키워 디테일을 채웠다. 렌더 방식은 규칙 7 그대로 — 로드 시 프레임별 프리렌더(색당 2.4ms), 비행 각도는 그리지 않고 좌우 반전만.
- **proj_sprites.js 신설**: 색 램프(흰 심→노랑→주황→빨강→암적 rim) 7종 × 혜성 화구 4프레임 + 창끝형 발광 화살 2프레임. 노이즈·불꽃 혀·불티 전부 시드 RNG — 같은 색은 항상 같은 그림.
- **bolt·orb → 혜성 화구, arrow·volley → 발광 화살**: 스킬 데이터는 안 바꿨다. shot.color가 램프를 고르므로 화염탄(빨강)·마력탄(보라)·빙결(파랑)·성광(금)이 자동으로 제 색 화구가 된다. 예전 줄무늬 경로는 sprite 미지정 시의 대체로 남겼다.
- **불티는 등간격이면 점선으로 보인다**: 첫 실측에서 확인. 개수 기반 해시 지터(Math.random 아님 — 재현 안정)로 간격·속도·수명·크기를 흩뜨리고 중력으로 떨어뜨렸다. 총구 섬광도 추가 — "발사됐다"는 인과.
- **화살 1차 형태는 실패였다**: 타원 촉 + 관통 축선 = "구슬에 꼬치 꽂은" 모양. 창끝형(뒤 1/3 최대 폭 → 앞으로 뾰족)으로 재설계하고 축빛을 촉 뒤로 한정했다. 확대 실측으로 잡은 것.
- 검증: 4종 동시 비행 + 실전 오토 전투(화염·빙결·궁수 파티) 완주 — 예외 0, 캐시 후 평균 0.17ms/프레임.
- **투사체 1차 반영 후 사용자 피드백 3건 (2026-08-05)**: ① 너무 작다 → 아트 픽셀 블록 2→3배(화구 120×66, 성물 4배), 수치는 PROJ_ART.basePx로. ② 화살같지 않다 → 촉·축·깃 3부위를 갖춘 실루엣으로 재작화. 실측 실패형 두 개를 기록한다: 깃이 촉보다 눈에 띄면 방향이 뒤집혀 보인다(깃은 짧고 어둡게), 축이 어두우면 촉·깃이 두 물체로 분리돼 보인다(축은 밝게 — 셋을 잇는 건 축이다). ③ **화살이 꽂히는데 참격 초승달이 그려진다** → 원거리 스킬 11개 전부가 fx: slash로 등록돼 있었다. 착탄은 burst로 교체(같은 색). 참격은 근접 17개에만 남는다. — 데이터 주도 구조 덕에 스킬 코드는 한 줄도 안 바꿨다.
- **투사체 2차 피드백 (2026-08-05)**: ① 3배가 크다 → 배율 대신 **아트 그리드를 축소**(화구 40×22→33×19 = 99×57, 화살 40×10→35×9). 배율은 정수만 쓸 수 있으니 미세 조정은 그리드가 맞다. ② 비행 중 **십자 별빛(twinkle)** — 불티 2개당 1개, sin 곡선으로 커졌다 작아지는 4방 십자 + 흰 심. ③ **시전 오라(spawnCastAura)** — 시위를 당기는 동안(p≥0, 발사 전) 시전자 발밑 마법진 + 몸으로 빨려드는 빛 9개 + 떠오르는 빛줄기 5개 + 몸통 파동. 치유의 가호(blessing)와 짝을 이루는 공격 연출. 수치는 fx.js CAST_AURA·TWINKLE.
- **투사체 3차 피드백 — 인과 정리 (2026-08-05)**: ① 절대영도가 wave(날아가는 검기)로 나가던 것 → bolt(빙결 화구). "마법이 참격으로 나가는" 마지막 케이스였다. wave는 이제 검사 성물 전용. ② 화살비·성궁 해방 — 가로로 쏘고 착탄에서 비(rain fx)가 내리던 인과 붕괴를, **대상 머리 위에서 세로 화살이 떨어지는** arrowRain으로 교체. 세로 화살은 런타임 회전 금지(규칙 7)라 구울 때 90° 돌려 굽는다(energyArrowFrames facing='down'). 착탄 fx는 rain→burst (화살 자신이 비가 됐으니). ③ 그물 투척·바람살 연사는 가로 연사가 맞으므로 착탄만 rain→burst. — rain fx를 쓰는 스킬이 0이 됐지만 셰이프는 남긴다(향후 낙뢰·유성 계열용).
- **다음 작업 방침 (사용자 지시)**: 이펙트는 "캐릭터 한 명씩 순서대로" 완성한다 — 주인공 걷기 원화 때처럼 하나를 끝까지 다듬고 다음으로. 순서: 주인공 궁수(이번에 절반 진행) → 주인공 마법사 → 동료 15명(에라 등 등장순).
## 주인공 궁수 원화 반영 — 캐릭터별 이펙트 패스 1번 완료 (2026-08-05)

- **13장 전부 발주서 파일명 그대로 도착** — 발주서에 파일명·포즈·검수 목록을 정확히 쓰면 AI 생성 쪽 관리 비용이 사라진다는 것을 확인.
- **tools/build_hero_archer.py 신설**: 검사·걷기 빌더에서 배운 것의 총합 — ① 가장자리 이웃 스프라이트 조각 제거(walk2·attack2·attack3·attack_up에서 실측) ② 명시적 방향 통일(공격 4장·상향이 오른쪽을 봐서 반전 — 활 뻗은 포즈는 자동 감지 불가) ③ idle0 "두꺼운 행" 높이(활 끝 같은 얇은 선 제외) 기준 세트 공통 배율 ④ 마지막 두꺼운 행 = 지면(y60) — 활 끝이 발 아래로 처져도 발이 뜨지 않게.
- **cast = attack_up 재사용** (검사의 cast=attack0 재사용과 동일 수법).
- **하늘 사격 자세 연결**: spec.sky(화살비·성궁)면 시전자가 attack_up으로 하늘을 향해 쏜다 — 화살이 위에서 떨어지는 인과의 발사 쪽 반쪽. 원화 없는 캐릭터는 cast 폴백이 attack0이라 저절로 무난.
- 상향 사격·풀 드로우의 활 끝이 캔버스 위에서 1~5px 잘리지만(세트 공통 배율 유지 대가) 실전에서 티가 안 난다 — 배율을 줄여 인물을 작게 만드는 것보다 낫다.
- **궁수 사격 모션 어색 (사용자 확인 2026-08-05)**: 원인 둘. ① attack3에 반전 누락 — 잔심에서 활이 반대 손으로 순간이동 (조립 실수). ② attack2가 발주 의도(릴리즈)와 달리 "메기는" 자세로 뽑혔다(활 수직·손이 가슴). **재생성 대신 재배열로 해결**: 뽑기(a0)→메기기(a2)→풀 드로우(a1)→발사·잔심(a3반전) = 실제 궁술 순서. 원거리 재생을 예비 3박자로 바꾸고(0.45/0.75/1.0), 화살이 그려진 프레임(풀 드로우)은 발사 전에만 나오게 했다 — 투사체가 나는데 손에 화살이 남는 모순 방지. SHOT_DRAW 0.22→0.30 (3박자를 읽을 시간). — AI 원화는 "의도한 포즈"가 아니라 "나온 포즈"로 조립하는 게 맞다.
- **궁수 v2 세트 반영 + 흰 테두리 제거 (2026-08-06)**: 사용자가 13포즈를 시트로 통째 재생성 — v2는 포즈가 발주 의도대로 나와 v1의 재배열([0,2,1,3])이 필요 없어졌다(파일 순서 = 궁술 순서로 복원). 시트는 연결 성분 분해로 낱장화(라벨 글자는 크기로 걸러냄). 방향은 은색(활) 중심 실측으로 확정 — **세트를 다시 뽑으면 방향이 또 달라진다**(v1: attack_up 반전 필요 / v2: 불필요, 대신 down 반전). ②흰 테두리: 순백 배경과 섞인 가장자리 픽셀이 투명 문턱을 넘겨 검은 외곽선 밖에 흰 띠로 남던 것 — "외곽선(어두움)을 만날 때까지" 밝기(luma>150) 기준으로 벗겨낸다. 얌전한 문턱(전채널>205)은 살색·회색 헤일로를 놓친다(실측 331px 잔존 → 2px). 검사·걷기 세트도 같은 헤일로가 있을 수 있다 — 눈에 띄면 같은 패스를 적용한다.
- **궁수 스킬 고유 연출 (2026-08-06, 사용자: "이펙트도 고유하게 화려하게")**: 색만 다른 범용 폭발을 스킬 정체성으로 분화. ① 정밀 사격·매의 표식 = pierce — 가로로 길쭉한 관통 섬광 + 대상에 과녁 문양(수축 링+십자 눈금)이 잠깐 남아 **표식(협공 2배)이 찍혔다는 메커니즘을 시각화**. ② 화살비·성궁 = 발사 순간 시전자 손에서 화살 한 발이 하늘로 솟는 라이저(스텝당 1발 — 광역 중복 방지) 추가, 올라감→쏟아짐 인과 완성. 세로 위 방향 화살은 구울 때 반시계 90° 회전. ③ 성궁 착탄 = holyburst — 폭발 위에 금빛 빛기둥+반짝이. 수치는 fx.js ARCHER_FX. 스킬 데이터는 fx 필드만 교체(데이터 주도).
- **시위 당김이 스킵돼 보임 (사용자 확인 2026-08-06)**: 원인은 그림 부족이 아니라 시간 — 당기는 구간이 2프레임 0.30초뿐. ① 활 계열(arrow·volley·arrowRain)만 예비 0.45초 (PROJECTILES.draw — 종류별 오버라이드 신설, 마법 응집은 0.30 유지). ② 당기는 몸짓: 예비 동안 몸이 표적 반대로 3px 가속 곡선으로 밀리다 발사 순간 +2px 반동 — 프레임 사이의 "당기는 과정"을 몸짓이 메운다. 그림 추가 없이 해결. AI 이미지 생성은 여전히 불가 — 중간 프레임이 정 필요하면 "반쯤 당긴" 1장을 발주가 답이지만, 시간+몸짓으로 충분한지 먼저 확인.
- **피격 찌그러짐 (2026-08-06, 작업계획 1부 7번 마지막 조각)**: 맞는 순간 발끝 고정으로 세로 16% 눌리고 가로 10% 퍼졌다 0.14초에 복원. 정수배 원칙(규칙 7)의 유일한 예외 — 과도 연출 한정. 수치는 fx.js SQUASH.
- **잿빛 숲 맵 톤 (2026-08-06)**: 이름이 잿빛인데 평원과 같은 쨍한 초록이었다. 맵 데이터에 tone 필드(데이터 주도) — 프리렌더에 1회 saturate/brightness 필터 + 잿빛 막. 1차(0.55)로는 초록이 우세해 실측 후 0.38/0.82/0.22로 확정 (채도 평원 0.708 → 잿빛숲 0.272).
## M5-1 커밋 ① — 거점 데이터·정산 모듈 (2026-08-06)

- **광산 Lv2 보간**(석6+철2), **마력샘 각성석 확률 3/5/8%/시간**, **건설비 "직전 단계 생산 10~15분 수급" 원칙**, 성벽 비용 ×3+마정석 — 기획서에 없는 수치의 산정 근거. 전부 balance.base.
- **정산은 1분 틱 루프** (최대 480회): 식량 고갈 경계를 수식으로 풀지 않는다. **8배 스케일 정수 누적**으로 규칙 6 유지 (0.25 일꾼 × 0.5 효율 = 1/8 최소 단위).
- **마력샘 재현성**: 시드 스트림 + 시행 횟수 카운터(springTicks) — 분할 정산 = 일괄 정산을 테스트로 보장.
- **잡은 버그**: 굶주림 판정이 "식량 0"이었다 → 인구 0인 진공 테스트가 반토막. "먹을 사람이 있는데 못 먹었다"로 수정. 테스트가 첫 실행에서 바로 잡았다 — 순수 모듈 + node 테스트 조합의 값어치.
- 자원 승격: state.field.resources → state.resources (7종: wood·stone·iron·food·mana·awaken·gold, herb→food 이관). ensureBase가 구세이브를 자동 변환.
## M5-1 커밋 ②③ — 거점 전경 씬 (2026-08-06)

- **전경 한 화면(카메라 없음)**: 기획서 "요새 전경 맵" — 960×540에 13시설 배치. 시설 그림은 지붕색 블록+이름표 코드 생성 임시 (drawBuilding의 블록부만 이미지로 교체 가능). 폐허(잠김)→공터 팻말(미건설)→건물(건설)로 상태가 실루엣에서 읽힌다.
- **방향키 포커스는 공간 탐색**: 그 방향 성분(along) + 수직 이탈 벌점(cross×2.2) 최소 — 목록 순서가 아니라 화면 배치대로 움직인다.
- **정산은 매 프레임 settle 호출** — 0분이면 무비용이라 타이머 관리가 필요 없다. 진입 시 1분 이상이면 "부재 중 생산" 패널.
- 씬 배선: 타이틀 새 여정/이어하기·필드 요새 출구 → placeholder에서 base로 교체. placeholder는 파일만 남김(미참조).
- 배치 실측 수정: 하단 생산 시설 y470이 자원 바(492)에 물렸다 → 전열 y384~404로. 세레스도 성벽 아래 지면(122)으로.
- 치트 6종 추가: 거점 진입·자원+1000·시간+1h/+24h·인구+5·진행 단계 올리기.
- **식량 경제는 농장 해금(2장)부터 가동 (사용자 지적 2026-08-06)**: 기획서의 농장 2장 해금 + 소비 0.5/분/명을 그대로 두면 1장 내내 적자 → 상시 -50%로 도시가 못 큰다. 수치 대신 **시작 조건**으로 해소 — 농장 해금 전엔 "왕실 배급"으로 소비·굶주림 없음 (하단 바에 표기). 서사 정합: 초반 피난민 소수 + 왕실 비축. 1장 채집 식량은 2장 대비 비축이 된다. 테스트 27케이스로 확장.
- **초반 건설 경제 재산정 (사용자 확인 2026-08-06: "얻는 재화에 비해 건설이 힘들다")**: ① 시작 물자 startKit(목80·석40·식60·금100) — 왕실이 내준 재건 밑천(서사 정합). 첫 벌목장+주택이 즉시 지어진다. ② 비용 곡선 원칙 교체 — Lv1은 "시작 물자+채집 몇 분", Lv2는 방치 10~20분, Lv3은 1시간. ★그 자원의 생산 시설이 해금되기 전 티어에는 그 자원을 조금만 부른다 (석재는 광산이 1장 후반 — 이전 표는 Lv2에 석재 80을 불러 공급원 없이 막혔다). ③ 채집 2-4 → 3-5. 성벽 관문 비용은 소폭만 완화 (챕터 게이트 무게 유지).
## M5-2 — 소환 제단(뽑기) (2026-08-06, 사용자: "가챠겜 스타일로")

- **로직·연출 분리**: core/gacha.js 순수 모듈 — 시드 스트림 + 시행 카운터로 분할=일괄 재현(마력샘 수법). 씬은 연출·입력만. node 테스트 15케이스 (확률 통계 2만회·천장 100회 스윕·중복 결정·결정성).
- **가챠 문법 풀셋**: 배너(카드 뒷면 부유+소환진) → 소환진 발광·입자 수렴 → 등급색 섬광(★3 금색+흔들림) → 카드 순차 플립 공개(가로 수축·확장 — 회전 금지 준수) → NEW!·중복 결정·천장 표기. 천장 게이지는 기획서대로 제단 Lv2부터.
- **초상은 전투 스프라이트 생성기 재사용** — 원화가 오면 그쪽이 우선되는 기존 구조 그대로.
- **시설 내용물 진입 규약**: buildings.js에 scene 필드 — 건설된 시설은 Z=입장, 증축은 B (제단이 첫 사례, 대장간·주점 등이 따라간다).
- 시작 물자에 증표 5 — 공급원(스토리·퀘스트)이 M6이라 그전 체험용.
- 잡은 버그: 마지막 공개 카드가 뒷면에 멈춤 (flipT가 공개 완료 후 정지) — 캡처 검증에서 발견.
- **뽑기창 고퀄 패스 (2026-08-06)**: 성운 배경(라디얼 블롭+은하수+별똥별) / 육망성 소환진(금·청 이중 삼각+역회전 룬+중심 빛기둥) / 부유 소환 크리스탈+궤도 입자 / 나선 수렴 / 등급색 방사 광선 섬광 / 카드 프레임 리디자인(등급별 프리렌더 — 이중 테두리·모서리 ◆·초상 후광·★2+ 방사선·이름 밴드) + 공개 직후 광택 스윕 + ★3 맥동 발광 + 픽셀 별 폴리곤 / 장식 버튼(호버 발광). 무거운 그림은 전부 프리렌더(규칙 7) — 프레임당 그리기는 룬·입자·스윕뿐.
- **공개를 수동으로 (블루아카 방식, 사용자 요청 2026-08-06)**: 자동 넘김 제거 — 클릭/Z로 한 장씩, [C]/버튼으로 모두 공개. **뒷면 등급 예고**: ★3은 금빛 맥동+라디얼 후광, ★2는 은은한 파랑 테 — "이 중에 금테가 있다"는 기대감이 수동 공개의 재미다. 다음 카드는 흰 테 맥동으로 안내.

## M5-3 육성 (2026-08-13)
- **성장 상태 위치**: 동료 = state.roster[cid]{lv,xp,lb,awaken}, 주인공 = state.heroGrowth{lv,xp}. ensureGrowth가 구세이브(lb만 있던 엔트리)를 지연 마이그레이션.
- **주인공은 한돌·각성 제외**: 한돌 재료가 "중복 뽑기"인데 주인공은 뽑기에 없고, 각성 규칙도 "전 동료 공통"(기획서 §4) — 동료 전용으로 읽음. UI에 사유 표기.
- **레벨 상한 = 15 + 훈련소Lv×15** (미건설 15 → Lv3 60). 기획서엔 "상한 확장"만 있어 챕터 페이스(ch2 훈련소 해금)에 맞춰 결정.
- **훈련소 골드 레벨업 비용 = 남은 필요 EXP × 0.4 (올림)** — 전투로 반쯤 채운 레벨은 싸진다. 전투 EXP가 주 경로, 훈련소는 벤치 따라잡기·마지막 한 렙 용도. 계수는 밸런스 재개 때 재점검.
- **전투 승리 EXP = Σ(적 레벨) × 8, 참전 전원 전액** (JRPG 관례 — 분배제는 교체 육성을 벌준다). 승리+실제 플레이(파티 지정)일 때만, 프리셋·시뮬 무영향.
- **한돌 비용(결정) = 캐릭터 등급** — 중복 1회 변환량(★3=3…)과 같아 "같은 캐릭터를 한 번 더 뽑으면 한 번 돌파"가 성립.
- **각성 비용 각성석 [4,8,16]** + 신전 건설(3장) 게이트. 공급이 마력샘 저확률뿐이라 보수적으로 — 정령의 시련 구현 때 재조정.
- **전투 연동**: createBattleFromStage에 growth 맵(옵션) — 아군 레벨·한돌·각성이 반영되고, 안 넘기면 기존 스테이지 레벨(시뮬 재현성 유지).
- **편성 로스터 연동**: 파티 화면은 보유 동료만 (state 없으면 전원 = 개발 폴백). 저장된 편성·스테이지 프리셋의 미보유 캐릭터는 걸러냄.
- 훈련소·신전 둘 다 육성 씬으로 진입 — 각성 축은 같은 화면에서 신전 건설 여부로 열린다 (화면 수 절약, 기획서 화면 13종 유지).
- **한돌 재료 개정 — 결정 폐지, 캐릭터 조각 (사용자 결정 2026-08-15)**: 중복 뽑기 = 그 캐릭터의 조각 +1 (등급 무관), 한돌 1회 = 조각 1개. 기획서 §7·§9의 "맹세의 결정(공용)" 방식을 대체 — "누굴 뽑았는지"가 그대로 그 캐릭터의 성장이 된다. 조각이 남은 돌파 횟수(완돌까지)를 이미 채운 캐릭터의 중복은 골드 변환(★1 150/★2 400/★3 1000) — 죽은 조각 방지. 구세이브의 resources.crystal은 ensureGrowth가 제거. **파장**: ★3 완돌이 순수 확률로는 어려워짐 → 기획서의 "잉여 증표 상점 교환" 원칙에 맞춰 M5 상점에서 증표→조각 교환을 넣어 보완 예정.
- balance.js의 죽은 중복 키 제거: M3 시절 growth(expBase)·gacha(rate3/limitBreakMax) 블록 — 뒤 정의에 가려 읽는 곳 없음 (죽은 손잡이 원칙).

## M5-4 장비 기획 확정 (2026-08-16, 사용자 결정 4건)
- 장착·인벤토리 UI는 **육성 화면 통합** (별도 씬 없음 — 캐릭터를 키운다 동선 일원화).
- 부옵션은 **스탯 6종만 먼저** (속성 데미지·협공·반격 위력은 데미지 훅+시뮬 재검증과 함께 다음 차수).
- 제작 연출은 **간단하게** (망치질+등급색 섬광 1회 — 반복 제작 피로 방지).
- **심볼전 승리 장비·강화석 드랍 포함** (제작 주 수급 + 드랍 부 수급, 시드 테이블).
- 상세 설계는 작업계획.md "M5-4 장비·인벤토리 기획안" — 구현 시작 전 단일 기준.

## M5-4 장비 구현 (2026-08-21)
- 기획은 작업계획.md M5-4안 그대로 구현 (사용자 확정 4건 + 이름표·시그니처). 구현 중 추가 결정:
- **주인공 직업 대응 장비**: 검사=대검·중갑, 궁수=활·경갑, 마법사=지팡이·로브 (기획서 "직업 대응 무기"의 방어구 확장 해석).
- **강화·등급 계수는 주스탯에만** — 시그니처(창방패 방어 등)는 고정값. 강화가 정체성 스탯까지 불리면 계열 간 격차가 계수 곱으로 벌어진다.
- **부옵 롤 = `${seed}:equip:${uid}` 스트림** (uid당 독립), 제작 등급 = `:craft` 스트림 1회 소비, 드랍 = `:drop` 스트림 **고정 4회 소비** — 분기별 소비 수가 다르면 되감기가 어긋난다.
- **드랍 티어 = ceil(스테이지 레벨 / 10)** (1~6) — 티어=챕터 대응의 근사.
- 결과 창 정보 줄은 "마지막 줄 y351 고정, 위로 쌓기" — 줄 수가 늘어도 버튼(y356)과 안 겹친다.
- 아이콘 티어 팔레트 스왑은 **하지 않는다** — 자체 작화가 풀컬러 64px라 스왑이 오히려 어색. 티어는 이름·T표기로, 등급은 테두리색으로 구분 (기존 계획 문구 갱신).
- 인벤 편의(잠금 토글·일괄 판매·NEW 표시·판매)는 상점 청크에서 (기록된 기존 결정 유지).

## M5-5 상점 (2026-08-23)
- **소모품(포션)은 이번 상점에서 제외** — 사용처(전투 중 아이템 UI)가 없는 상품을 먼저 팔면 죽은 기능.
  "소모품 7종 + 전투 아이템 사용(행동 소모·전투당 3회)"을 별도 청크로 작업계획에 기록.
- 상점 구성: 장비 완제품(하급 T1·중급 T2, **일반 등급 고정** — 상위 등급은 제작·드랍 몫) /
  강화석 낱개·묶음 / **조각 교환**(증표→보유 동료 조각, ★1=1·★2=2·★3=4 — 2026-08-15 개정의 보완책 이행) /
  장비 판매(제작가×등급계수×0.35, 장착 중·잠금 보호).
- 경제 정합을 테스트로 고정: 상점가 > 제작가(확정 수급 프리미엄), 판매가 < 상점가(되팔이 차익 없음),
  ★3 완돌 조각값 20증표 < 천장 30회 (지정 구매가 천장보다 싸야 보완책이 된다).
- 조각 구매도 뽑기 중복과 같은 "죽은 조각 방지" 규칙 적용 (완돌까지 필요한 만큼만).

## M5-6 소모품·전투 아이템 (2026-08-24)
- 소모품 7종(기획서 §10) 데이터·가격·전투 사용 구현. 효과 수치는 balance.items —
  포션은 **최대 HP 비율**(30/60/100%) 회복: 고정값은 레벨이 오르면 썩는다.
- **부활 깃털 = 첫 사망 아군 자동 대상** — 시체 타겟팅 UI 회피(작업계획 결정 이행).
  부활한 턴에는 행동 불가(acted), 상태이상·보호막은 초기화.
- 비약은 기존 상태(atkUp·ironwall)에 ratio·turns 오버라이드로 부여 — 새 상태 없음.
- 이벤트 재생은 기존 타입(heal·status·cleanse) 재사용, revive만 신규 (fade 복구 + 축복 마법진).
- 아이템도 행동 소모, 전투당 3회(balance.battleItems). 오토는 아이템을 쓰지 않는다(단순함 유지).
- 상점 구매 탭 상단에 소모품 진열 (물약 실루엣은 코드 생성 임시 — 아이콘 발주 후보).

## M5-7 교감 (2026-08-24)
- 기획서 §12 그대로: Lv1~5(낯섦→맹세), 대화 1일 1회(+6)·선물 1일 1회(적중 +20/그 외 +8)·
  전투 승리(참전 동료 +2, 조용히 — 결과 창 줄 수 절약). 필요 포인트 [20,30,45,60].
- 보상 중 Lv2 +3%·Lv4 +7%만 실동작 (계단식) — 전투·편성·육성 스탯에 실반영.
  Lv3 전용 의뢰·Lv5 비하인드는 잠금 표기만 (서브퀘·스토리 마일스톤 몫).
- **대사는 임시 초안** (사용자 지시 2026-08-24: "대사 작업은 나중에, 틀만") —
  data/bonds.js TALKS(15명×3구간)가 교체 지점. 화면에도 "임시 초안" 주석 표기.
- 선물 6계열×일반 등급만 (고급은 추후), 취향은 기획서 §4 표 그대로. 상점 구매 탭 진열.
- 쿨타임은 실시간 24h (세금과 동일 문화) — 치트로 초기화 가능.

## 문서 정리 (2026-08-22, 사용자 지시)
- 루트에는 핵심 5개만: CLAUDE.md · 기획서 · DECISIONS.md · 작업계획.md · CREDITS.md.
- **발주/** 폴더 신설 — 에셋 발주 문서 전부 이동: 일러_카드/(구 일러_발주), 장비아이콘.md,
  작화_규격서.md, 원화_전체.md(구 원화_생성_프롬프트), **전투원화_주력3인/**(신규).
- 보관 문서는 docs/: 스토리_집필_패키지.md, 적킷_설계보관.md.
- 전투원화 발주는 PNG 한 장이 아니라 **폴더(지침서.md + 브란_참고일러.png + 그림체_참고_검사시트.png)**
  — 일러_카드 발주와 같은 패턴 (사용자 정정 반영).

## 2026-08-22 — 밸런스 재역산 1차 (3직업 기준선)

- **재개 절차 ①② 완료**: PASS_CRITERIA 난이도 보정(±15%p, **하한만** 이동 — 상한 "너무 쉬움"
  판정은 노말 전용. 하드에서 저레벨 스테이지가 수월한 건 결함이 아니다) + 9칸 격자 헬퍼
  (`judgeGrid`, `node tools/judge_grid.mjs`). 앞으로 밸런스 판정은 격자가 기본.
- **암흑 주술사 mag 12→7**: mag는 마법 '방어'를 겸한다(logic.js 315행) — 12면 마법사 파티가
  못 뚫는 벽. 자힐도 1.5/쿨2→1.1/쿨3 (단독 생존 시 자힐 스톨 11/턴이 저딜 파티를 영원히 막았다).
- **브란 도발 2턴** (selfBuffTurns 채널 신설, 기본 status.taunt 1턴은 유지 — 적 망령의 포효는
  그대로): 커버율 33%→66%. 이것으로 세라핀 사망률 96%→0, 유리몸 주인공 2v2 생존 정상화.
- **마법사·궁수 주인공 상향**: mage mag [10,1.5]→[11,1.9]·basic 1.15·a1 1.05→1.3 /
  archer atk [8,1.4]→[9,1.55]·a1 1.4→1.6. 순수 화력이 유리몸·저유틸의 대가라는 정체성.
- **성직자 hp +3, 도적 hp +2**: 사수·교활의 유리몸 사냥 2턴을 버티는 하한.
- **잣대 재조정**: skirmish 하한 0.70→0.50·턴 [2,3.5] / normal 하한 0.55→0.40·턴 [3,5].
  근거 — 도발 탱커가 흡수하는 파티의 최저HP는 최소보장 칩댐 하한(~0.42)에 수렴하고,
  캐스터 파티는 검사 돌격보다 구조적으로 ~1턴 느리다. 직업 개성을 잣대가 벌하지 않게.
  검사 전용으로 잡혀 있던 1차 튜닝 기준의 3직업 포락선 보정이다. 판정에 ±0.1턴 허용오차.
- **격자 결과**: 검사 7/7·7/7·5/7, 마법사 5/7·5/7·5/7, 궁수 5/7·5/7·3/7 (이지·노말·하드).
  남은 불합격은 2개 스테이지로 수렴 — ① 망령 기사장(보스): 직업 격차(검사 6.2턴 vs 캐스터
  9~10턴)가 기획서 앵커(5~8턴) 폭보다 넓다 → 앵커 완화 또는 캐스터 보스킷 보강, 사용자 판단 필요.
  ② 기습 방어전: 오토가 버티기에서도 닥돌해 사망 ~3 (기준 2) → 버티기 전용 오토(대기 활용) 또는
  스테이지 물량 완화. 다음 세션 과제.

## 2026-08-22 — 성물 필살기 「여명 해방」 구현 (기획 승인분)

- 여명 게이지제: 시작 25, 행동 +25·피격 +15·아군 전투불능 +40·협공 +8, 만충 100에 해방.
  초기 22/12/30 시안은 짧은 전투에 한 번도 안 닿아 상향 (T3 전후 첫 해방 리듬).
- 성물 계수 1.0~1.1/쿨3 → 2.4/게이지. 오토의 "적 2체 이상" 게이트 제거 — 보스 단독에도 발동.
  효과: 보스전 직업 격차 수렴 (마법사 9.4→8.3턴·궁수 10→8.2턴), 보스 전투불능 0.8→0.7 이하.
- 지표 반전 주의: 보스전 사망이 줄자 예전엔 죽어서 집계에서 빠지던 녹스가 5% 생존으로
  minHp에 잡혔다 — 도적 hp +2 추가(총 +4)로 20% 선 보정. 남은 격자 불합격은 기존 미결
  (보스 잔여·버티기 오토·하드) 그대로.
- 연출: 기존 성물 cine(암전·차지·섬광·흔들림·프리즈) 위에 해방 컷인 추가 —
  확대 스프라이트 잔광 슬라이드 + 스킬명 타이포 임팩트. 컷인 중 턴 배너 숨김.
  전용 효과음은 M8 보류.
- UI: 주인공 HP바 위 여명 스트립(만충 금색 맥동), 스킬창 성물 카드 "여명 N% / 만충 — 해방 가능!".
- 기획서 §7 "쿨 3" → "여명 게이지 만충 시" 개정 (사용자 승인).

## 2026-08-22 — 밸런스 재역산 2차 완결: 3직업×3난이도 9칸 격자 전 칸 합격

게임 측 변경:
- **버티기 생존 본능** (ai.js): victory=survive에서 HP 55% 이하면 공격 대신 웅크림
  (막타 가능한 적만 예외). 버티기 사망 3→1 이하로.
- **도르간 킷 정합**: 「대지의 성채」에 도발 추가 + 보호막 스케일 마력→방어(스킬·패시브).
  자기만 지키는 기사 = 죽은 정체성이었고, 버티기 고정 파티의 붕괴 원인이었다.
  브란과 차별: 브란 = 도발 2턴 상시형 / 도르간 = 도발 1턴 + 시작 보호막 + 철벽.
- 보스 역할 atk 1.05→1.0 (무탱 파티 칼끝 생존 13%→20%대), 도적 hp 25→33 누적 +8.
- 마법사: 직업 기본 마력 10→11(동료 카시안·에라 포함), 주인공 mag [11,2.0]·a1 1.4.
- 궁수: 주인공 기본 공격 10·a1 1.7.
- ★교훈: **측정 구간(Lv2~3)에선 성장치 소수점 조정이 반올림에 씹힌다** — 기본치로 조정할 것.

잣대 측 변경 (3직업×3난이도 포락선으로 §5 재해석):
- 소규모전 턴 상한 3.5→4 / 일반전 하한 0.35 (탱커 칩댐은 전투 길이에 비례)
- 버티기 상한 판정 제거 — 생존자 HP는 버티기 지표가 아니다, deaths가 드라마를 잰다
- 난이도 스케일: 이지 minHp +10%p(원안 15 — deaths가 본질 안전판)·턴 하한 ×0.95 /
  하드 minHp −15%p·턴 상한 ×1.3(적 스탯 +15%의 기계적 귀결)·deaths ×1.5(위험이 정체성)
- 판정 ±0.15턴 허용오차

결과: **9칸 63판정 전 칸 합격** (판당 200판). 재개 절차 ③ 완료 — ④ 적 킷 착수 가능.
남은 별도 판단 2건은 그대로: 반격 postDefCoef 통일 여부, 표식·방어약화 grace.

## 2026-08-23 — 전투 스프라이트 해상도 2배 (192×128)

사용자 지적: "주인공만 대두고 다른 캐릭터는 얼굴이 작아 형태가 안 보임 — 캐릭터 크기를
키워 픽셀 해상도를 높여라." 원인 분해: 화면에는 몸 116px로 그리면서 소스는 58px(×2 확대)
이라, 얼굴이 몇 픽셀 안 되는 동료·적은 형태가 뭉개졌다 (주인공만 원화 자체가 대두라 생존).

- 결정: **재작화가 아니라 변환 해상도 상향** — 캔버스 96×64 → 192×128 (c01 288, 기사 240),
  몸 58→116px, 화면엔 1:1로 그림. 화면 크기 불변, 밀도 2배, 정수배 원칙에는 오히려 더 부합.
  960×540 논리 해상도에서 1 아트픽셀 = 1 논리픽셀 — 이 이상은 낭비라 2배가 상한.
- 엔진: 등록표에 `res: 2`, 로더가 캔버스에 assetRes 스탬프, 소비처 6곳(전투·컷인·편성·
  육성·교감·뽑기 2곳)은 `sprScale()`로 나눠 화면 크기를 유지. 코드 생성 캔버스는 무영향.
- 파이프라인: 상수 ×2 일괄(섬 제거 문턱은 면적 등가 ×4: 4→16). 주인공 검사는 PS1을
  파이썬(c02 파이프라인)으로 승계 — 소스가 RGBA 투명 배경이라 알파=마스크 특례,
  플립은 git 구판과 **색상** 대조로 자동 판정 (실루엣이 거의 완전 대칭이라 실루엣 대조 불가).
  궁수도 흰 캔버스+벗기기 구파이프라인을 프리멀티플라이드로 현대화 (헤일로 원천 제거).
- 뽑기 공개 연출 1곳만 비정수 배율(5/2=2.5)이 됐다 — 고밀도 소스라 체감 미미, 관찰 항목.

## 2026-08-23 — 해상도 2배 후속 수정 2건 (사용자 보고)

- **필드 옆걸음 파손**: 필드 등록표가 전투 walk0~3.png를 공유하다 전투 파일이 192×128이
  되며 필드에서 거대+발 꺼짐. → 변환기가 **필드 전용 96×64 사본(walk_side0~3)**을 따로
  출력하고 등록표를 그쪽으로 전환 — 파일 분리로 두 화면의 규격이 서로를 다시는 못 깬다.
- **세라핀 둠칫 크기 펄스**: idle1 원화가 idle0보다 작게 생성돼 있었다 (실측 c03 -12%,
  해골병사 -8% — 구판부터 있던 문제인데 해상도가 올라가며 눈에 띔). → 숨쉬기 프레임만
  idle0 두꺼운행 높이에 정규화(NORM_TO_IDLE0). 자세 차이는 유지되고 크기만 맞는다.
  "세트 공통 배율" 원칙의 예외가 아니라 원칙의 목적(프레임 간 크기 일관성) 그 자체다.

## 2026-08-23 — 주인공 검사 v2 세트 (등신 통일 재발주분 적용)

12장 수령(1254×1254 흰 배경). 포즈 매핑: (1)대기 (2~5)걷기 (6)치켜들기 (8)웅크림
(7)돌진임팩트 (9)잔심 — 공격 시퀀스를 예비→압축→방출→복귀로 재구성. (10)성물
시전(발광검·유일한 왼쪽보기) (11)피격 (12)무릎꿇기. idle1은 미발주라 합성(상체 2px).

- 걷기 4장이 89.9~96.9%로 제각각(원화 크기 불일치) → idle0 높이 정규화 (c03 전례 확장)
- attack0·cast의 검끝 상단 소량 잘림은 수용 — 하늘을 찌르는 연출로 읽히고 본체 무결
- cast가 구판의 attack0 재탕에서 전용 포즈(성검 상향)로 분리 — 여명 해방 컷인 직결
- 필드 옆걸음(walk_side)도 v2 원화에서 재생성 — 필드·전투 화풍 동시 전환
- 남은 구세대 그림: 필드 정면·후면 걷기 16장 (2차 발주 대기, 지침서 6절)

## 2026-08-24 — 외곽선 흰 점 제거 (사용자 보고)

원인: 소스의 검정 외곽선↔흰 배경 사이 안티에일리어싱 픽셀(회백색)이 잉크 판정(luma<235)을
통과 → 축소 후 밝은 회색 낱점으로 외곽에 잔존. 구해상도에선 축소 평균에 뭉개져 안 보이다가
2배 해상도에서 드러남.

- **1차 시도 폐기**: 소스 해상도에서 밝은 경계를 외곽선까지 벗기기(luma>100, 6회) —
  섬네일에서 얇아진 외곽선을 뚫고 은발(카시안)·금 지팡이(세라핀)를 파먹었다.
  카시안 v1 은발 사고와 동일 원리의 재발. **"경계 벗기기는 색 문턱만으로는 안전하지 않다"**
  — 이 파이프라인이 프리멀티플라이드로 간 이유를 재확인.
- **채택**: 출력 단계 국소 수술 — 투명에 접한 밝은(luma>150) 픽셀 중 **연결 3px 이하로
  고립된 것만** 가장 어두운 이웃 색으로 교체 (지우면 핀홀 — 눌러 붙인다).
  드레스 자락·검날·뼈처럼 밝은 재질의 긴 실루엣 띠는 연결 크기로 생존. 구조 무손상 실측.

## 2026-08-24 — 왕녀 세레스 외형 확정 (일러 1호 승인)

지침서 초안(은백발 긴 생머리·무기 없음)과 달리 **금발 업두+은 티아라, 허리에 의전용 검**으로
생성됐으나 수용: ① 화풍이 승인 카드 일러들과 동계열(검수 몽타주 합격) ② 세라핀과
헤어스타일(업두 vs 풀어헤침)로 구분 충분 ③ 의전용 검은 "마지막 왕족의 유산, 본인은 싸우지
않는다"는 서사 소품으로 오히려 득. 지침서 4-2절을 승인본 기준으로 갱신 — 이후 재발주는
이 외형이 정본. assets/illust/npc_seres.png, NPC_ILLUSTS 등록(M6 VN에서 소비).
